# public/art/ — real, loaded game art assets

This directory holds real PNG (and, from Phase 74 onward, PNG+JSON atlas)
assets loaded through Phaser's normal texture pipeline, per the approved
visual overhaul plan at `docs/phase_73_to_78_visual_overhaul_plan.md`.

Everything here is served by Vite directly from `public/` (no build-time
processing), at `/art/<file>` in dev and copied verbatim into `dist/art/` on
`npm run build`.

## Current contents

| File | Status | Notes |
|---|---|---|
| `tiles-atlas.png` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-tiles.mjs` (flat colors + a border/corner marker per frame, NOT real art). 160x32px, 5 frames of 32x32px each, in `TileType` enum order: Dirt, Gravel, Sand, Water, Rock. |
| `buildings-atlas.png` + `buildings-atlas.json` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-buildings.mjs` (flat color per `BuildingCategory` + lightened 1px border + a small deterministic corner marker/footprint-size tick per building, NOT real art). 1920x64px, 37 frames packed side-by-side: 34 base building frames (11x 32x32 for 1x1 footprints, 23x 64x64 for 2x2 footprints) plus 3 appended 32x32 variant frames (`building-House-tier2`, `building-House-tier3`, `building-WoodenGate-closed`). Frame names exactly match `buildingTextureKey()` in `src/config/buildingConfig.ts`. |
| `animals-atlas.png` + `.json` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-units.mjs` (flat color per animal kind + lightened border + darkened corner marker, NOT real art). 72x18px, 4 frames of 18x18px each: `animal-Chicken`, `animal-Pig`, `animal-Cow`, `animal-Ostrich`. |
| `cowboys-atlas.png` + `.json` | **PLACEHOLDER** | Single 18x18px frame `cowboy`. |
| `mounted-cowboys-atlas.png` + `.json` | **PLACEHOLDER** | Single non-square 24x18px frame `cowboy-on-horse` (4:3 width:height ratio). |
| `brawlers-atlas.png` + `.json` | **PLACEHOLDER** | Single 18x18px frame `brawler`. |
| `dynamiters-atlas.png` + `.json` | **PLACEHOLDER** | Single 18x18px frame `dynamiter`. |
| `villagers-atlas.png` + `.json` | **PLACEHOLDER** | Single 18x18px frame `villager`. |
| `raiders-atlas.png` + `.json` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-raiders.mjs` (flat color per faction + lightened border + darkened corner marker, NOT real art). 54x18px, 3 frames of 18x18px each: `raider-Outlaws`, `raider-Rustlers`, `raider-Coyotes`. `raider-Coyotes` additionally gets a red "bandana" stripe marker (see §5f note below) distinguishing it from `wildlife-Coyote`. |
| `raider-camps-atlas.png` + `.json` | **PLACEHOLDER** | 72x24px, 3 frames of 24x24px each: `raider-camp-Outlaws`, `raider-camp-Rustlers`, `raider-camp-Coyotes`. Each has a distinct tent-canvas base color AND a distinct top-left corner icon (Outlaws: diagonal rifle line; Rustlers: small corral-outline square; Coyotes: scattered 3-dot bone pile) per the plan's §5g "differentiate beyond tent colour" recommendation. |
| `wildlife-atlas.png` + `.json` | **PLACEHOLDER** | 54x18px, 3 frames of 18x18px each: `wildlife-Snake`, `wildlife-Coyote`, `wildlife-MountainLion`. `wildlife-Coyote` shares `raider-Coyotes`' base tan/brown color family but deliberately has NO bandana marker - see §5f note below. |
| `vegetation-atlas.png` + `.json` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-world.mjs` (flat color per kind + lightened border + darkened corner marker, NOT real art). 64x32px, 2 frames of 32x32px each (tile-sized, unaffected by the 18px small-unit bump): `vegetation-Tree`, `vegetation-Cactus`. |
| `carts-atlas.png` + `.json` | **PLACEHOLDER** | Single non-square 14x10px frame `goods-cart` (`CART_SPRITE_WIDTH`/`HEIGHT`, independent literals not derived from `ANIMAL_SPRITE_SIZE`). Gets the shape-readability stripe treatment described below since its aspect ratio is wide/thin. |
| `accents-atlas.png` + `.json` | **PLACEHOLDER** | 144x24px, 6 frames of differing sizes packed side-by-side: `accent-WellCrank` (16x4), `accent-WarehouseDoor` (24x24), `accent-SupermarketAwning` (64x8), `accent-ChickenDoor` (16x12), `accent-HouseWindowLight` (12x12), `accent-Campfire` (12x12). Sizes are read directly from each accent's real MainScene.ts frame usage (not invented) - see the "Accents" note below. |
| `resource-icons-atlas.png` + `.json` | **PLACEHOLDER** | Generated by `tools/generate-placeholder-resource-icons.mjs` (flat color per resource, chosen for a natural association where one exists - red for meat, blue for water, brown for wood/logs, grey for stone/iron/tools, near-black for coal, etc. - plus lightened border + darkened corner marker, NOT real art). 180x12px, 15 frames of 12x12px each (`RESOURCE_ICON_SIZE`, a HUD-chrome size independent of Phase 75/76's 12->18 small-unit-sprite bump): one per `ResourceKey` (`resource-icon-rawMeat`, `-meat`, `-water`, `-eggs`, `-leather`, `-clothes`, `-logs`, `-wood`, `-potatoes`, `-liquor`, `-agaveJuice`, `-stone`, `-iron`, `-tools`, `-coal`). |

Phase 76 raised `WILDLIFE_SPRITE_SIZE` (`src/config/wildlifeConfig.ts`) from
12px to **18px**, matching Phase 75's `ANIMAL_SPRITE_SIZE`/`RAIDER_SPRITE_SIZE`
bump (this constant was deliberately left at 12px in Phase 75, deferred to
Phase 76 since it's an independent literal, not an alias). Like
`ANIMAL_SPRITE_SIZE`, this is a texture-generation/loading size only -
wildlife sprites are placed centred and never `setDisplaySize`d. Real
AI-generated art for `wildlife-atlas.png` must be authored at 18x18, not the
old 12x12. `RAIDER_CAMP_SPRITE_SIZE` (24px) is unaffected by this change - it
is an independent literal, not part of the 12px small-unit size class.

**§5f note (raider-Coyote vs. wildlife-Coyote differentiation):** the plan's
§5f resolved that these two must be visually distinguishable even as
placeholders, so a future real-art pass has the pattern already established.
Both share the same base tan/brown color (`[0xbf, 0xa9, 0x80]`) so they still
read as "the same animal", but `raider-Coyotes` alone gets an extra horizontal
red bandana-stripe band near the top third of the frame - a placeholder stand-
in for the plan's real-art direction of giving the raid-faction Coyote a
bandana/scar marker. When real art replaces this atlas, replicate the same
concept (a bandana, scar, or similar hostile-marking accent on the raid
version only) rather than two identical Coyote sprites.

Phase 75 also raised the small-unit texture-generation size from 12px to
**18px** (`ANIMAL_SPRITE_SIZE` in `src/config/buildingConfig.ts`, and its
aliases `VILLAGER_SPRITE_SIZE`/`COWBOY_SPRITE_SIZE`/`BRAWLER_SPRITE_SIZE`/
`DYNAMITER_SPRITE_SIZE`/`RAIDER_SPRITE_SIZE`; `MOUNTED_COWBOY_SPRITE_WIDTH`/
`HEIGHT` scaled proportionally 16x12 → 24x18). This is a real, permanent
rendering-size change independent of the placeholder-vs-real-art distinction
above — real AI-generated art for these 6 atlases must be authored at 18x18
(24x18 for the mounted-cowboy frame), not the old 12x12/16x12.

**Accents note (Phase 77, plan §5b.2 accent carve-out alignment risk):** the
6 accent frames above are pieces conceptually "carved out of" their parent
building sprite, positioned at hardcoded pixel offsets in `MainScene.ts`
(e.g. `WellCrank` at `originX+16, originY+2`; `Campfire` at `originX+4,
originY+46`, outside the Barracks' 64x64 footprint, in its yard). Because
`buildings-atlas.png` is itself still Phase 74 PLACEHOLDER flat-color art (not
final art), there is no real "hole" in the current building sprites for these
placeholder accents to visually plug — Phase 77's job was only to get each
accent's frame SIZE and NAME correct (so `MainScene.ts`'s existing
`createAccentImage`/per-accent wrapper methods and their `setOrigin`/tween
calls, all byte-identical to before this phase, resolve to a correctly-sized
image) and to prove the non-square shapes (`16x4`, `64x8`) round-trip through
the atlas packer correctly. A **future** phase — after both real building art
AND real accent art exist — must verify actual pixel alignment (does the real
`WellCrank` art actually sit over a real crank-shaped cutout in the real Well
sprite?); that verification was NOT possible in Phase 77 and was not
attempted. When authoring real accent art, author the building + its accent as
one image first, then split it (per the plan's §5b.2 mitigation), rather than
generating them independently.

## Why a placeholder

Phase 73 built the complete loading pipeline (folder structure, `BootScene`
load path, `gameConfig`'s `pixelArt: true`, the loading progress bar, and the
`tools/verify-asset-dimensions.mjs` dimension-check tool) but no
AI-image-generation tool was available in the environment that implemented
it. `tiles-atlas.png` is a functional stand-in only — correct file path,
correct dimensions, correct frame order/count — so the tilemap renders and
the pipeline can be proven end-to-end. It is **not** an attempt to replicate
the target art style described in the plan's §2 (hand-painted pixel art,
locked Western palette, top-left lighting, etc.).

## Before shipping this overhaul for real

Replace `tiles-atlas.png` with a real, AI-generated 160x32px, 5-frame strip
following the plan's §2 art direction spec and §3.4 generation pipeline, in
the exact same frame order (Dirt, Gravel, Sand, Water, Rock — this order is
load-bearing, see `src/config/mapConfig.ts`'s `TileType` enum and
`src/scenes/MainScene.ts`'s `map.addTilesetImage` call). After replacing the
file, re-run `node tools/verify-asset-dimensions.mjs` to confirm the new file
still matches the expected 160x32px before committing.

Replace `buildings-atlas.png` + `buildings-atlas.json` with real, AI-generated
art per the plan's §2 art direction spec (one building per `BuildingType`,
plus the 3 House-tier/WoodenGate variant frames), keeping every frame's exact
pixel dimensions (32x32 for a 1x1 footprint, 64x64 for a 2x2 footprint — see
`BUILDING_DEFINITIONS[type].size` in `src/config/buildingConfig.ts`) and exact
frame name (`buildingTextureKey()` in the same file is the source of truth —
do not rename frames). A hand-packed JSON atlas is fine to keep using (see
`tools/generate-placeholder-buildings.mjs` for the JSONHash shape Phaser 4's
`this.load.atlas()` expects), or repack with a real texture-packer tool if the
new art isn't laid out in one row. After replacing, re-run
`node tools/verify-asset-dimensions.mjs` (whole-file dimension sanity) AND
`node tools/verify-building-frames.mjs` (per-frame name + per-frame size
correctness against every `buildingTextureKey()` output) before committing.

Replace `animals-atlas.png`/`.json`, `cowboys-atlas.png`/`.json`,
`mounted-cowboys-atlas.png`/`.json`, `brawlers-atlas.png`/`.json`,
`dynamiters-atlas.png`/`.json`, and `villagers-atlas.png`/`.json` with real,
AI-generated art per the plan's §2 art direction spec, at exactly 18x18px
(24x18px for the mounted-cowboy frame — non-square, 4:3 ratio) and exactly
the frame names listed above (`animalTextureKey()`/`COWBOY_TEXTURE_KEY`/
`MOUNTED_COWBOY_TEXTURE_KEY`/`BRAWLER_TEXTURE_KEY`/`DYNAMITER_TEXTURE_KEY`/
`VILLAGER_TEXTURE_KEY` in `src/config/buildingConfig.ts` are the source of
truth — do not rename frames). §5b.1 of the plan applies here specifically:
these sprites are mirrored via `setFlipX` for facing, so author any
asymmetric identity cue (a holster, a lit fuse) either centred or duplicated
on both sides. After replacing, re-run `node tools/verify-asset-dimensions.mjs`
(whole-file dimension sanity) AND `node tools/verify-unit-frames.mjs`
(per-frame name + per-frame size correctness) before committing.

Replace `raiders-atlas.png`/`.json` and `wildlife-atlas.png`/`.json` with real,
AI-generated art per the plan's §2 art direction spec, at exactly 18x18px, and
exactly the frame names listed above (`raiderTextureKey()`/`wildlifeTextureKey()`
in `src/config/buildingConfig.ts`/`src/config/wildlifeConfig.ts` are the source
of truth — do not rename frames). Preserve the §5f Coyote differentiation
described above: `raider-Coyotes` must remain visually distinguishable from
`wildlife-Coyote` (bandana/scar or similar), not an identical reused sprite.
Replace `raider-camps-atlas.png`/`.json` with real, AI-generated art at exactly
24x24px per frame (`raiderCampTextureKey()` is the source of truth), preserving
the §5g per-faction differentiation (each camp should read as visually distinct
beyond just its tent-canvas color). After replacing any of these three, re-run
`node tools/verify-asset-dimensions.mjs` (whole-file dimension sanity) AND
`node tools/verify-raider-frames.mjs` (per-frame name + per-frame size
correctness) before committing.

Replace `vegetation-atlas.png`/`.json` with real, AI-generated art per the
plan's §2 art direction spec, at exactly 32x32px per frame (tile-sized, NOT
the 18px small-unit size), and exactly the frame names listed above
(`vegetationTextureKey()` in `src/config/vegetationConfig.ts` is the source of
truth — do not rename frames). Replace `carts-atlas.png`/`.json` with real art
at exactly 14x10px (`CART_TEXTURE_KEY` in `src/config/buildingConfig.ts`).
Replace `accents-atlas.png`/`.json` with real art at each accent's exact
pre-existing frame size (`accentTextureKey()` in the same file is the source
of truth — do not rename frames): `accent-WellCrank` 16x4, `accent-
WarehouseDoor` 24x24, `accent-SupermarketAwning` 64x8, `accent-ChickenDoor`
16x12, `accent-HouseWindowLight` 12x12, `accent-Campfire` 12x12 — see the
"Accents note" above regarding the real alignment verification this still
owes once real building art exists. After replacing any of these three, re-run
`node tools/verify-asset-dimensions.mjs` (whole-file dimension sanity) AND
`node tools/verify-world-frames.mjs` (per-frame name + per-frame size
correctness, including independent width/height checks for the non-square
frames) before committing.

Replace `resource-icons-atlas.png`/`.json` with real, AI-generated art per the
plan's §2 art direction spec (adapted for tiny HUD glyphs - a single readable
silhouette per resource rather than a detailed illustration, since only 144px
survive at 12x12), at exactly 12x12px per frame and exactly the frame names
listed above (`resourceIconTextureKey()` in `src/config/buildingConfig.ts` is
the source of truth — do not rename frames). After replacing, re-run
`node tools/verify-asset-dimensions.mjs` (whole-file dimension sanity) AND
`node tools/verify-resource-icon-frames.mjs` (per-frame name + per-frame size
correctness) before committing — or simply run `npm run verify:art`, which
runs every one of this project's 6 art-verification scripts (including these
two) in one command.

## Phase 78 — pipeline migration complete

Phase 78 (the last of the six-phase visual overhaul) replaced
`generateResourceIconAtlas()` with a real loaded PNG+JSON atlas and, since it
was the last remaining procedural generator in `BootScene.ts`, also deleted
that file's shared procedural-drawing primitives: `drawPixelSprite()`, the
`PixelSprite`/`PixelPalette` types, `PIXEL_GRID`/`PIXEL_SIZE`,
`ANIMAL_PIXEL_GRID`, and `RESOURCE_ICON_SPRITES`. `BootScene.ts` now consists
entirely of `this.load.image(...)`/`this.load.atlas(...)` calls (14 atlases:
tiles, buildings, animals, cowboys, mounted-cowboys, brawlers, dynamiters,
villagers, raiders, raider-camps, wildlife, vegetation, carts, accents,
resource-icons), the Phase 73 loading-progress-bar wiring, and
`publishBuildingIcons()`/`scene.start('MainScene')` in `create()` — down from
its original ~1919 lines to well under 200.

**Every code path in the game now loads real image files.** All 15 atlases
listed in this README are still PLACEHOLDER art (flat colors + border/marker,
not the plan's §2 hand-painted pixel-art style) — the pipeline migration is
complete, but the actual finished artwork still needs to be produced (via the
plan's §2/§3 art-direction spec and generation pipeline) and dropped into
`public/art/` in place of each placeholder, verified each time with
`npm run verify:art`, before this overhaul is visually complete.
