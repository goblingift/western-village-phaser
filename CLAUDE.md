# Project: Western Village (Phaser 4)

## Goal

Small 2D building/management game in the browser (Phaser 4, TypeScript, Vite).  
Player places buildings (cattle farm, butcher, well, house, road) on a tilemap and optimizes simple production chains in a Western setting.

## Tech Stack

- **Phaser 4** (2D Game Framework)
- **TypeScript** (strict mode)
- **Vite** (Build Tool)
- **ESLint + Prettier** (Code Quality)
- **Git** (Version Control)

## Game Scope (Minimal)

### Map
- Fixed map: 40x30 tiles
- Tile types: Grass, Water, Sand

### Buildings
- Cattle Farm (produces raw meat)
- Butcher (processes raw meat + water → meat)
- Well (produces water)
- House (provides population/workforce)
- Road (connects buildings, optional production bonus)
- Optional: Warehouse (resource buffer)

### Resources
- Money (start: ~500)
- Raw Meat
- Meat
- Water

### Game Loop
1. Place buildings (cost in money/resources)
2. Automatic production per tick (every 2 seconds)
3. Simple UI: resource display, building selection, placement mode
4. Goal: Maximize meat production with limited space & resources

## Coding Standards

- **TypeScript strict**: No `any` types without compelling reason
- **Clear Interfaces**: Type all data structures
- **Modularity**: One feature per file/module
- **Comments**: Only for complex logic, code should be self-explanatory
- **Build**: After each feature, `npm run build` must pass without errors

## Workflow Rules for Claude Code

1. **Plan before Implementation**: For new features, first propose a plan (affected files, changes, risks), then wait for approval
2. **Focused Sessions**: One feature per session or clearly separated block
3. **Git Branches**: Each feature in its own branch, review diffs like code review
4. **Performance**: For >50 buildings use culling + pooling, avoid heavy calculations in update loop unless necessary
5. **Documentation**: Briefly document new features in this file under "Feature History"

## Performance Rules

- Viewport Culling: Only render visible tiles/buildings
- Object Pooling: For recurring objects (building visuals, particles)
- Texture Atlases: All building sprites in few large textures
- Separate Layers: Render ground, buildings, UI in different layers
- Reduced Update Rate: Don't update every object every frame

## Feature History

| Date | Feature | Description |
|------|---------|-------------|
| 2026-09-05 | Project Setup | Initial Phaser 4 + TypeScript + Vite setup |
| 2026-09-05 | Tilemap & Camera | Code-generated 40x30 tilemap (grass/water/sand), draggable camera, tile-coord readout |
| 2026-09-05 | Building Placement | Building definitions, placement preview with validity check, DOM building bar, money deduction |
| 2026-09-05 | Resources & Production | Global resource pools (raw meat/meat/water), 2s production tick per building, resource HUD, building info panel |
| 2026-09-05 | Roads & Logistics | Road-network connectivity (BFS through road chains) grants +10% production output, green outline on connected buildings |
| 2026-09-05 | Goal & Score | 5-minute countdown, cumulative meat score, game-over overlay with building counts, Play Again reset |
| 2026-09-05 | Polish & Performance | Building sprites packed into one texture atlas, connection outlines merged into a single shared Graphics object, throttled tile-info HUD updates, Western color palette, tooltips, procedural placement sound |
| 2026-09-06 | Bank | New staffed 2x2 Bank building with a per-building `bankBalance`: deposit/withdraw $50 buttons in the info panel, 0.5% compounding interest per production tick (runs regardless of staffing, halted only while disabled at 0 HP), and a raid-risk tie-in — once every Bank's combined balance reaches $200, raid faction pick shifts to 60% Outlaws/20% Rustlers/20% Coyotes and the next-raid interval is halved (unchanged below that threshold) |
| 2026-09-05 | Cowboy Unit Control | Cowboys refactored from Barracks-derived slot sprites into independently tracked `CowboyUnit`s with live x/y; left-click selects one (ring highlight + HUD hint), right-click issues a point-to-point move order (clamped to map bounds); raid combat now fires from each unit's live position instead of a fixed slot |
| 2026-09-05 | Barracks & Cowboy Units | Barracks building (2x2, staffed, no production) trains garrisoned Cowboy units ($40 each, up to 3, own atlas/sprite, static slot near the Barracks, no wander); Cowboys have their own regenerating HP (`cowboyHp` array on the Barracks' `PlacedBuilding`) and a `COWBOY_RANGE_TILES` constant reserved for Phase 23's combat |
| 2026-09-05 | Villager Characters | POPULATION_PER_HOUSE pixel-art villagers spawn per placed House (capped at 30 rendered sprites), wandering forever between random placed buildings via chained point-to-point tweens with directional flip; cleaned up on game-reset |
| 2026-09-05 | Livestock Economy | Chicken Farm, Pig Farm, Cattle Farm and Cow Ranch now start with 0 animals and produce nothing until stocked; `buyAnimal()` buys one animal per click (hard-gated on an adjacent Fence, the per-building animal cap, and money), with per-tick output scaling as `outputPerAnimal * animalCount`. Retired the old Cow-Ranch-only "half output without a Fence" rule (`requiresFence`) in favor of this buy-gate. Building info panel shows Animals: count/max and a Buy button with a disabled reason (no Fence / at cap / can't afford). |
| 2026-09-05 | Warehouse & Autonomous Logistics | Removed click-to-collect (Phase 8): production now flows straight into the global resource pool each tick, capped by a global storage cap (`BASE_STORAGE_CAP` + `WAREHOUSE_STORAGE_BONUS` per staffed Warehouse); added 2x2 Warehouse building (workers required, no production output) reusing Phase 12's workforce-assignment pass; HUD shows the current storage cap |
| 2026-09-05 | Fences for Cattle | Adjacent Fence tiles now render as a connected fence line (dedicated Graphics layer, right/down adjacency check in gameState's `getFenceLinks`); Cow Ranch's fenced state (full vs half output) is surfaced in the building info panel via `hasAdjacentFence` |
| 2026-09-05 | Visual Overhaul (Pixel Assets) | Replaced flat-color tiles/buildings with procedurally generated pixel-art textures: each tile/building is drawn on an 8x8-per-tile logical pixel grid (BootScene.ts `drawPixelSprite`) and scaled to TILE_SIZE, keeping the same TILESET_KEY/BUILDING_ATLAS_KEY contract |
| 2026-09-05 | Minimap | Fixed 200x150px minimap (5px/tile) below the resource HUD, top-left; flat-color terrain + per-building-type colored dots redrawn on `building-placed`/`game-reset`, a separately throttled camera-viewport rectangle updated on drag/click, and click-or-drag-to-navigate scoped to the minimap's screen rect via `isPointerInMinimap`/`minimapPointerActive` guards in the existing pointer handlers |
| 2026-09-05 | Population & Workforce | Houses grant `POPULATION_PER_HOUSE` (2) population capacity each; every production building's `workersRequired` is derived from its footprint (`getWorkersRequired` = width×height) so it stays DRY as new building types are added; each tick `gameState`'s `assignWorkforce` auto-staffs buildings in placement order, first-come-first-served, and a building only produces when fully staffed (`staffed`/`assignedWorkers` on `PlacedBuilding`); HUD shows `Population: employed/total`, building info panel shows `Workers: assigned/required` |
| 2026-09-05 | Supermarket & Autonomous Sales | Added 2x2 Supermarket (cost 200, `requiresWorkers: true`, no `production` field); each tick, after normal production, a new `runSupermarketSales` pass in `gameState.ts` has every staffed Supermarket sell up to `SUPERMARKET_SELL_RATES` (2 Meat @$5, 2 Eggs @$3) out of the global pool into Money — `active`/`lastSale` on a Supermarket reflect whether it actually sold something this tick rather than the normal input/output check; building info panel shows "Sold: X Meat, Y Eggs -> +$Z" or why it isn't selling |
| 2026-09-05 | Western Art Pass | Content-only pass over `BootScene.ts`'s pixel sprites: wood-plank wall texture on every walled building, saloon-style false-front roof on House, windmill/crank detail on Well, longhorn-horns motif + hitching rail distinguishing Cow Ranch from Cattle/Pig Farm, enhanced barn/general-store detailing on Warehouse/Supermarket, wagon-wheel-rut Road, sparse cracked-earth/cactus flecks on Grass/Sand tiles — same `PixelSprite`/`drawPixelSprite` renderer, no logic changes |
| 2026-09-05 | Animal Sprites (Static) | Owned animals are now visible: three small critter sprites (Chicken/Pig/Cow) drawn on a separate, coarser 6x6 logical grid scaled to a 12px `ANIMAL_SPRITE_SIZE`, packed into their own `ANIMALS_ATLAS_KEY` atlas (same `drawPixelSprite` renderer, now parameterized by pixel size). `MainScene`'s `BuildingVisual` tracks each building's `animalImages[]`; `redrawAnimalSprites` (re)creates `building.animalCount` static Images at deterministic per-index slots in a row beneath the footprint (wrapping into further rows as needed), depth 11 above the building's depth-10 sprite — driven only by placement and the `animal-bought` event, never by `production-tick`. Cleaned up alongside the rest of a building's visuals on `game-reset`. |
| 2026-09-05 | Animal Walking Animation | Animals now wander instead of sitting frozen: `startAnimalWander` (called once per Image right after creation in `redrawAnimalSprites`) gives each animal sprite a single looping `yoyo: true, repeat: -1` tween drifting it between its Phase-17 slot anchor and a randomized point ~10-12px away on X (plus a subtle ±4px Y bob), with randomized duration (900-1600ms) and start delay (0-1000ms) per animal so a group doesn't move in lockstep. No new frame art: facing direction is faked via `setFlipX`, toggled in the tween's `onYoyo`/`onRepeat` callbacks — the two points where the movement direction actually reverses. Tweens are killed with `this.tweens.killTweensOf(animalImage)` right before each `animalImage.destroy()`, both in `redrawAnimalSprites`'s destroy-and-recreate loop and in `setupGameReset`'s cleanup loop, so no tween ever targets a destroyed GameObject. |
| 2026-09-05 | Building Idle Animation | Five building types get a small ambient accent, created once in `tryPlaceAt` (`createBuildingAccents`) and tracked in `BuildingVisual.accentObjects[]`: Well's crank bar (carved out of `WELL_SPRITE` into its own `ACCENTS_ATLAS_KEY` frame) swings -15°/+15° via a center-pivot yoyo rotation tween; Warehouse's hay-loft door (also carved out of the base sprite) swings 0°/+8° from a top-edge pivot (deviated from the doc's symmetric center-rotation suggestion — a top-hinged swing read as an actual door at this scale, a center swing just looked like wobbling); Supermarket's awning (carved out) sways via a 0.95x/1.05x `scaleX` yoyo tween; Chicken Farm's coop opening (carved out) flaps via a 1.0/0.3 `scaleY` yoyo tween with a per-building-randomized duration (600-900ms) and `repeatDelay` so multiple coops don't flap in sync; House gets 3 small `Arc` circle "smoke puffs" near the roofline, each on its own `y`-rise + fade-to-0 tween with `repeat: -1` (which auto-resets start values) and a staggered per-puff start delay. All new atlas accents (`WellCrank`/`WarehouseDoor`/`SupermarketAwning`/`ChickenDoor`) live in a new `ACCENTS_ATLAS_KEY` atlas (`BootScene.generateAccentAtlas`, same `drawPixelSprite` renderer, variable-width layout like `generateBuildingAtlas`); the corresponding detail was removed from each base `BUILDING_SPRITES` entry so it isn't drawn twice. Cleanup follows the exact `killTweensOf` + `.destroy()` pattern Phase 18 added for animal sprites, now also applied to `accentObjects` in `setupGameReset`. |
| 2026-09-05 | HP & Regeneration | Every `BuildingDefinition` gets a tiered `maxHp` (Road 15, Fence 20, House/Well/ChickenFarm 45-50, the four 2x2 farms/Butcher 80, CowRanch/Warehouse 100, Supermarket 90); `PlacedBuilding.hp` starts at `maxHp` on placement and regenerates `ceil(maxHp * 2%)` per tick unconditionally (`runHpRegen` in `gameState.ts`, run even for staffed=false/inactive/0-HP buildings, since regen is the only recovery path). 0 HP means "disabled": the production loop early-continues on `hp <= 0` before the staffing check, `assignWorkforce` skips 0-HP buildings entirely (zeroing `assignedWorkers`/`staffed`, recomputed fresh every tick so a building that just hit 0 HP is corrected the same tick), and `getStorageCap` excludes disabled Warehouses from the staffed-Warehouse bonus. Building info panel shows `HP: current/max`, red/bold with an "(Disabled)" suffix at 0 HP (`.hp-disabled` class in `index.html`). `MainScene` draws a small two-layer HP bar (depth 13, above villagers) over any building currently below `maxHp`, hidden at full health, redrawn every `production-tick` alongside the other per-tick Graphics passes. No damage source exists yet (Phase 23) — this phase is HP/regen/gating plumbing only. |
| 2026-09-05 | Raid Events & Combat | Three fictional threat factions (`RaiderFaction`/`RAIDER_DEFINITIONS` in `buildingConfig.ts` — Outlaws: 30hp/6dmg/50px-s, targets any building; Rustlers: 25hp/5dmg/50px-s, prefers farm buildings with an `animal` config, falling back to any; Coyotes: 15hp/3dmg/75px-s, same farm preference) raid on a self-rescheduling random 45-90s timer (`MainScene.scheduleNextRaidCheck`, skips spawning if a raid is already active, always re-picks a fresh random delay). A triggered raid picks one faction at random and spawns 2-5 units (own `RAIDERS_ATLAS_KEY` sprites, same 12px small-unit size class as animals/villagers/Cowboys) at a random point along a random map edge; each raider commits once to its nearest valid target building (per its faction's targeting rule, re-picked only if the current target dies or was never found) and walks straight there via a single Phase-20-style point-to-point tween, then stops and attacks in place. Combat resolution (`MainScene.runRaidCombatTick`) rides the existing 2s production-tick cadence rather than `update()`: raiders in range deal damage straight to `PlacedBuilding.hp` (gameState remains the source of truth for building HP, floored at 0 — Phase 21's existing disable/regen gates handle the rest unmodified), and every living Cowboy (`cowboyHp[i] > 0`) fires `COWBOY_DAMAGE` (8) at its own nearest raider within `COWBOY_RANGE_TILES`, with a cheap fading `Graphics` line as the shot visual. Raider state (hp/faction/target/tween) lives entirely in `MainScene` as a local `Raider[]`, not in `gameState.ts` — ephemeral/wave-scoped and tween-heavy, unlike core building/resource state. A wave ends when every raider is dead or after a 60s timeout, whichever first; a HUD notice ("Outlaws incoming!" etc., same `add.text(...).setScrollFactor(0).setDepth(1000)` style as the resource/timer HUD) shows for the wave's duration. `game-reset` cancels the pending raid-check timer, ends any active wave, and destroys all raider/shot visuals before rescheduling a fresh randomized raid timer. |
| 2026-09-06 | Multi-Select (Drag Rectangle) | Generalized Phase 24's single-Cowboy selection into a `CombatUnit`/`selectedUnits[]` model (forward-compatible with Phase 28's second unit kind) and swapped the camera-pan control from left-drag to right-drag, freeing left-drag for a drag-rectangle multi-select box. `setupCameraDrag` now gates panning on explicit `pointer.rightButtonDown()` rather than the old bare `pointer.isDown` (which is true for *any* held button, confirmed against Phaser's own Pointer typings); a left-drag past `CLICK_MOVE_THRESHOLD` instead draws a semi-transparent world-space `Graphics` rectangle (`updateSelectionRectangle`, no `setScrollFactor(0)` so it tracks the ground correctly as the camera scrolls mid-drag) via `MainScene`'s `dragStartWorldX/Y` snapshotted at `pointerdown`. Move orders moved from `pointerdown` to `pointerup` (gated on `pointer.rightButtonReleased()` + the same distance threshold) so a right-click-to-command can be told apart from a right-drag-to-pan; a qualifying right-click now issues one move order per selected unit, each with a small ±12px random offset (`issueUnitMoveOrders`/`issueUnitMoveOrder`) so a multi-unit order doesn't stack every unit on one point. `redrawSelectionRing` now draws one ring per selected unit. Building placement, building-info-panel selection, minimap click/drag/navigate, raid combat, and single-unit select-then-move all continue to work unchanged. |
| 2026-09-06 | Leather/Clothes, Wood, Potato Chains | Three new independent production chains reusing existing patterns, no new mechanics. Cattle Farm's and Cow Ranch's cow `outputPerAnimal` gained a second key (`leather: 0.05`/`0.1` per animal per tick) alongside their existing `rawMeat`, scaled the same way via `scaleByAnimalCount`. Four new staffed 2x2 buildings in `BUILDING_DEFINITIONS`: `Sewery` (cost 130, maxHp 80, `{inputs: {leather:1}, outputs: {clothes:1}}`, Butcher-shaped), `Forestry` (cost 60, maxHp 45, `{outputs: {logs:1.2}}`, flat like Well), `WoodCutter` (cost 100, maxHp 80, `{inputs: {logs:1}, outputs: {wood:1}}`), `PotatoField` (cost 90, maxHp 45, `{outputs: {potatoes:1.2}}`, flat, no `animal` config — a crop field, not livestock). Five new `ResourceKey`s (`leather`/`clothes`/`logs`/`wood`/`potatoes`) threaded through `Resources`/`resetGame`/`RESOURCE_LABELS` in both `buildingConfig.ts` and `BuildingInfoPanel.ts`/the resource HUD. `SUPERMARKET_SELL_RATES` widened from a `meat`/`eggs`-only `Record` to a `SupermarketSellableKey` union (adds potatoes $2, wood $3, clothes $15); `runSupermarketSales` and `SupermarketSale` were generalized to loop over that table and store a `sold: Partial<Record<SupermarketSellableKey, number>>` map instead of hardcoded `meat`/`eggs` fields, so `BuildingInfoPanel.formatSaleText` and `describeBuilding`'s Supermarket tooltip line now read every configured good generically. Resource HUD (`MainScene.formatResourceText`) grew from 2 lines to 3 (money/population/storage, then 5 resources, then 4) to avoid collision — the minimap's Y position already derives from `resourceText.height` so it re-flowed automatically. Four new pixel-art sprites in `BootScene.ts` (`SEWERY_SPRITE`: cloth bolt + hide-drying rack; `FORESTRY_SPRITE`: pine-tree stand + log pile, no walls; `WOOD_CUTTER_SPRITE`: grey tin roof + circular saw blade + log stacks; `POTATO_FIELD_SPRITE`: tilled furrows + potato sprigs/tubers, no walls). |
| 2026-09-06 | Liquor & Saloon | New `liquor` `ResourceKey` closes out Phase 26's potato chain. `Liquor Still` (cost 140, maxHp 80, staffed, `{inputs: {potatoes:2}, outputs: {liquor:1}}`, Butcher-shaped) turns potatoes into liquor; `Saloon` (cost 200, maxHp 90, `requiresWorkers: true`, no `production`) autonomously sells it. Rather than widen `SUPERMARKET_SELL_RATES`/`runSupermarketSales` again, Saloon got its own parallel `SALOON_SELL_RATES` (`{liquor: {amount:2, price:12}}`) and `runSaloonSales` pass in `gameState.ts` (called right after `runSupermarketSales` in `runProductionTick`), writing to a new `saloonSale` field on `PlacedBuilding` instead of reusing `lastSale` — two buildings sharing one rate table/field would make a future change to one sellable-goods list risk the other. The two sale shapes (`SupermarketSale`/`SaloonSale`) were unified into one generic `AutoSale<K extends ResourceKey>` in `buildingConfig.ts` so `BuildingInfoPanel.formatSaleText` stays a single generic method serving both buildings' "Sold: X Liquor -> +$Y" / "Not selling (understaffed)" / "No stock to sell" display — a type-level generalization only, `runSupermarketSales`'s own logic/output is untouched. Resource HUD's third line gained a fifth entry (Liquor) alongside Clothes/Logs/Wood/Potatoes. New `LIQUOR_SPRITE` (copper still + stacked barrels, Butcher-window idiom) and `SALOON_SPRITE` (stepped false-front parapet, hanging sign, upstairs window row + balcony rail, batwing doors) in `BootScene.ts`. |

| 2026-09-06 | Terrain, Water & Vegetation | Map generation rewritten in `mapConfig.ts`: `TileType` is now Dirt/Gravel/Sand/Water (grass is gone — the basin is dry), ground painted as overlapping ragged patches rather than per-tile noise, and water grown as 2-4 frontier-expanded lake blobs plus one random-walk river (~3-5% of tiles). The generated grid became module state (`getWorldTiles`/`getTileTypeAt`/`isBuildableTerrain`/`distanceToNearestWater`) instead of a MainScene local, because `gameState` must consult terrain on every placement check without depending on the scene. New vegetation layer: `config/vegetationConfig.ts` (kinds/densities/terrain/yields) + `state/vegetation.ts` holding real `VegetationEntity` records (id/kind/tile/remainingYield) that block building placement, render as tile-sized sprites at their own depth (5, under buildings) and appear on the minimap; the module never imports `gameState` (replanting takes an `isTileFree` predicate from the caller instead), so there is no cycle. `canPlaceBuilding` finally consults terrain — the new `getPlacementRejection` is the single source of truth returning a player-facing reason, with `canPlaceBuilding` a thin boolean wrapper so preview and placement can't disagree. Wells are hard-gated on being within `WELL_MAX_WATER_DISTANCE_TILES` (3) of water and their per-tick output scales down with that same distance via `WELL_OUTPUT_BY_DISTANCE`. |
| 2026-09-06 | Destruction, Repair & Mortal Units | `runHpRegen` deleted outright: free 2%-per-tick healing meant raids had no lasting cost. Damage is now permanent until paid for (`repairBuilding`, cost pro-rated by missing HP against `REPAIR_COST_FRACTION` of build cost, surfaced as a Repair button in the info panel), and 0 HP means destroyed rather than "disabled forever". One shared `removeBuilding` path backs both raid destruction (`destroyBuilding`, no refund) and the new bulldozer (`demolishBuilding`, `DEMOLISH_REFUND_FRACTION` 50% back): it frees occupancy, drops the record from `placedBuildings`/`buildingsById`, recomputes road connections, and emits `building-removed` — workforce needs no explicit recompute since `assignWorkforce` already rebuilds from scratch every tick. The building is removed from state *immediately* (gameState stays the single source of truth) and MainScene plays the shake + fade + dust-burst destruction animation on the now-orphaned sprite, following the existing `killTweensOf`-then-`destroy()` discipline. Units are mortal: raiders now prefer any Cowboy/Cowboy-on-Horse within `RAIDER_UNIT_ATTACK_RANGE_TILES` over their building target, damage routes through `gameState.damageUnit`, and a dead unit's HP slot is kept at 0 (rather than spliced) so surviving units' `CombatUnit.index` alignment holds while only the count decrements, freeing a training slot. Demolish mode is a building-bar toggle (click buildings to tear down repeatedly) plus a per-building Demolish button. Raid escalation generalizes Phase 29's bank hook into `getThreatLevel()` (elapsed time + net worth, blended 50/50): wave size scales toward `RAID_MAX_UNITS_ESCALATED`, raider HP toward `RAID_MAX_HP_MULTIPLIER` (carried per-raider, never mutating `RAIDER_DEFINITIONS`), the interval squeezes, factions bias Outlaw past `OUTLAW_BIAS_THREAT`, and a countdown notice warns `RAID_WARNING_LEAD_MS` ahead. Fixed a latent crash the destruction path finally made reachable: `pickVillagerTarget` indexed an empty building list once a town could lose every building. |
| 2026-09-06 | Harvest Chains, Upkeep & Net Worth | New `HarvestConfig` on `BuildingDefinition` (kind/radius/yieldPerTick/outputs/replantChance) lets a building draw output from real vegetation instead of conjuring it: Forestry lost its flat `production.outputs` and now fells actual Trees within 5 tiles (depleting and removing them, slowly replanting at `replantChancePerTick`), and the new 2x2 staffed **Cactus Milker** (cost 150) harvests Cacti into a new `agaveJuice` `ResourceKey` sold at the Saloon via the existing `runSaloonSales` pass — no new selling logic. `getWorkersRequired` counts `harvest` as production so harvesters staff correctly. Wood-cutter deliberately stays a pure logs→wood mill: gating it on trees too would double-gate one chain. Building upkeep (`upkeep` field, `runUpkeep`) drains money per tick from staffed buildings; a town that can't pay flags the building `disabled` for that tick — idled, never deleted — and recomputes fresh each tick like `assignWorkforce`, so it self-heals the moment cash returns. `STARTING_MONEY` raised 500 → 1800 to absorb upkeep, repairs and rebuilding. Scoring replaced: `computeNetWorth()` (cash + banked + resource stock priced at `RESOURCE_VALUES` + standing building value) drives both the HUD headline and a `NetWorthBreakdown` on the game-over screen. |
| 2026-09-06 | UI Overhaul: Bar, Zoom, Speed, HUD | Building bar rebuilt from a flat 21-button wrapping row into category tabs (new `BuildingCategory` on each definition, so a new building can't be added without placing it in the menu) with icon buttons: BootScene rasterises each frame of the existing `BUILDING_ATLAS_KEY` into a data URL and publishes it through `ui/buildingIcons.ts`, wrapped in try/catch so a failed canvas read degrades to text labels rather than breaking the bar. Unaffordable buildings dim (rather than hide/disable) and clicking one still selects it so the placement preview can explain the problem — that preview now shows *why* a spot is invalid via a world-space hint label fed by `getPlacementRejection`. Mouse-wheel camera zoom clamps to `CAMERA_MIN_ZOOM`/`MAX` and zooms to cursor by re-anchoring the pre-zoom world point; the effective floor is raised to whichever is larger, the configured minimum or the map-fits-viewport zoom, since zooming out past that just frames the map in dead space. Pause + 1x/2x/4x drive Phaser's global `time.timeScale`/`tweens.timeScale` rather than rescaling individual timers, so production, the clock, raids and every animation stay in lockstep. Resource HUD extracted to `ui/ResourceHudPanel.ts` (MainScene was already the largest file and owns raids/units/input/visuals): a laid-out panel with a header line and a two-column icon grid over all 11 resources, each with a `+X.X/tick` trend from a new per-tick `resourceTrends` delta in gameState; the minimap derives its Y from the panel height. The canvas gained a positioned `#stage` wrapper so the info panel anchors to the play area instead of the viewport — the taller bar had been overlapping it and swallowing clicks on the speed controls. |
| 2026-09-06 | Horsery & Cowboy on Horse | New `Horsery` building (cost 220, maxHp 100, `requiresWorkers: true`, no `production`) trains up to 2 "Cowboy on Horse" units ($90 each) via `trainMountedCowboy()`, mirroring Barracks' `trainCowboy()` but with its own parallel `mountedCowboyCount`/`mountedCowboyHp` fields on `PlacedBuilding` (kept separate from Barracks' `cowboyCount`/`cowboyHp` so the two buildings' unit pools never collide). `CombatUnit` (MainScene.ts) gained a `kind: 'cowboy' \| 'cowboyOnHorse'` discriminant plus a small `UNIT_KIND_CONFIG` lookup carrying only what differs between kinds — walk speed (`MOUNTED_COWBOY_WALK_SPEED_PX_PER_SEC` = 3x the plain Cowboy's); range/damage/combat logic stay shared, since a mounted unit fights identically to a regular one, just faster and tougher (`MOUNTED_COWBOY_MAX_HP` 40 vs 30). Both kinds already flow through Phase 25's generalized `cowboyUnits`/`selectedUnits` selection, box-select, and move-order code untouched. New `HORSERY_SPRITE` (stable/corral: rail fence, hay bale, horse-head silhouette) and a non-square `MOUNTED_COWBOY_SPRITE` (16x12, wider than the Cowboy's 12x12 to read as horse+rider) in `BootScene.ts`, its own single-frame atlas. |
| 2026-09-06 | Day/Night Cycle & Run Structure | The one-shot `GAME_DURATION_SECONDS` countdown is replaced by a repeating cycle: `DAY_PHASE_SECONDS` 150 + `NIGHT_PHASE_SECONDS` 150 = a 300s cycle, run `DAY_COUNT` (3) times for a ~15-minute run at 1x, after which the existing net-worth scoreboard fires unchanged. gameState now tracks a single forward-counting `elapsedSeconds` and *derives* day number and phase from it (`getPhaseAtElapsed`/`getDayNumberAtElapsed`/`getPhaseRemainingSeconds`) rather than keeping a second, separately-decremented counter that could drift from the day count; `tickTimer` detects a boundary by comparing the derived phase before and after the increment and emits `day-phase-changed`. `getThreatLevel()` was rebased off the (now non-existent) shrinking `remainingSeconds` onto elapsed time — same 0→1 curve over a run, but it no longer breaks the instant the clock repeats. New defeat condition: `removeBuilding` ends the run early when the last building falls, gated on `reason === 'destroyed'` only (bulldozing your own last shed is a rebuild step, not a loss), so `GameOverSummary` gained `reason: 'time' \| 'destroyed'` and `daysSurvived` and the overlay shows "Town Destroyed!" vs "Time's Up!". Visuals live in a new `ui/NightOverlay.ts` — one `setScrollFactor(0)` viewport-sized rect at depth 900, above the world (buildings 10, HP bars 13) but under the HUD band at 999/1000 so the panels stay legible at midnight, alpha-tweened 0↔`NIGHT_OVERLAY_ALPHA` (0.45) over a `DAY_NIGHT_TRANSITION_MS` (20s) dusk/dawn; it snaps rather than fades on `game-reset`, since resetGame emits `day-phase-changed` just before it and a 20s dawn out of last run's midnight would be a bug. Night polish reuses Phase 19's `ACCENTS_ATLAS_KEY` system rather than inventing a second one: new `HouseWindowLight` and `Campfire` accent frames are created once with a building's other accents, held at alpha 0 through the day and faded in at dusk (so a phase change is a tween on existing objects, not a create/destroy churn across every House), plus a cool `VEGETATION_NIGHT_TINT` multiply on every tree/cactus. HUD reads `Day N - Daytime/Night M:SS` ("Daytime" rather than "Day", because "Day 2 - Day 1:40" read as a typo in testing). |
| 2026-09-06 | Raid Grace, Harvest Reporting & Cactus Balance | Three diagnosed bugs fixed. **(1) Raids started almost immediately** — `getThreatLevel()` reads ~0.15 from the starting purse alone at t=0, so the first wave landed 41-83s in, before a Barracks was affordable. Two combined gates now apply, both re-checked at fire time (a timer's delay can span a phase boundary): a hard `RAID_EARLIEST_ELAPSED_MS` (300s) floor and night-only spawning, making the earliest possible raid the first nightfall at/after 5:00 — night of day 2. An ineligible check simply reschedules rather than banking a raid to fire the moment the gate opens, and the countdown warning is only scheduled when `willRaidBeEligibleIn()` predicts the wave will actually be allowed. **(2) The "no trees/cacti nearby" message was wrong three ways** — the panel inferred it from `lastHarvest`, which only `runHarvest` writes and which sits behind the hp/staffing/upkeep gates in `runProductionTick`, so a freshly-placed, understaffed or unpaid harvester was told it had no vegetation. `BuildingInfoPanel.describeHarvestStatus` now calls `countVegetationInRadius` (written in Phase 32, until now dead code with zero call sites) and reports blockers in the order they actually apply: destroyed → understaffed (with the worker count) → upkeep unpaid → nothing in range → running-with-stock. Placement gained a *soft* `getPlacementWarning` (advisory, never a block — the preview stays green and only the hint's colour changes) so a harvester isn't blind-placed on bare ground, and both share a new `getHarvestCenterTile` with `runHarvest` so the reach the UI describes and the reach the tick uses can't diverge. `VegetationDefinition` gained `pluralLabel` after testing surfaced "No Cactuss in range". **(3) Cactus Milker was unshippable** — 3-11 cacti map-wide meant it drank its radius dry in 16-64s and then sat dead forever. Cactus density 0.035 → 0.12, `maxYield` 8 → 14, sand raised to a larger share of the map (mapConfig's ground-patch roll is now weighted, `PATCH_TYPE_WEIGHTS`), and the Milker got Forestry-style replanting (`replantChancePerTick` 0.1) — the old "finite desert bounty, no replant" rule was a nice idea that made the building unusable. Measured after: 38-43 cacti at generation, and a Milker harvesting on 100% of ticks across a 150-tick session with its in-range count holding steady or rising. |
| 2026-09-06 | Audio Engine, World-State & Visibility | `audio/sound.ts` grew from 27 lines (one bare lazy `AudioContext`, one square-wave placement blip straight to `destination`) into a real engine, because this phase's sounds are timer-driven, stackable and world-positioned rather than one click-triggered blip: a master `GainNode` every voice routes through (one node for mute/volume instead of N call sites scaling their own gain), explicit unlock on first `pointerdown`/`keydown` plus `visibilitychange` (a context created outside a gesture starts suspended and a backgrounded tab re-suspends it — both silent failures), a hard `MAX_CONCURRENT_VOICES` budget with per-sound `minGapMs` throttles, viewport culling (an off-screen world sound creates no nodes at all) and `StereoPannerNode` panning by x-offset from the camera centre, and game-speed awareness (0x mutes; 2x/4x deliberately do *not* get an extra trigger-rate multiplier on top of the already-faster timers). Sounds are procedural recipes (filtered noise bursts for gunfire/collapse, ramped tones for calls/confirms). Wired up: combat (cowboy fire collapses into one louder "volley" voice above `VOLLEY_SHOT_THRESHOLD` shooters instead of 10-15 individual voices, else staggered 0-150ms; raider-hit, unit-death, building-collapse pairing with Phase 31's destruction animation), unit movement (one move-order confirm per *order* not per unit; footsteps only for selected units mid-tween, globally throttled — per-step audio for all ~30 wandering villagers/animals was ruled out as unusable), and animal ambience (one shared self-rescheduling timer picks one on-screen critter per firing rather than 30 independent timers all firing at once). Mute toggle + volume slider added to the building bar's top row. Separately: **the world no longer runs behind the game-over screen** — MainScene never listened for `game-over`, so raiders kept chewing through buildings and every tween kept running; a listener now ends the wave, cancels all raid/ambience timers and zeroes `time.timeScale`/`tweens.timeScale` (restored on Play Again). `setupSpeedControl`'s comment claimed `runProductionTick` early-returns while paused — it does not, it only returns on `gameOver`; the guard was latent (timeScale 0 stops the timer firing at all) but is now real, in the tick callback, and the comment corrected. **Harvest radius made visible**: a square ring (square, not a circle — `countVegetationInRadius`/`findNearestVegetation` use a Chebyshev radius, and a circle would picture a rule the game doesn't implement) drawn at depth 6 under the buildings on both the placement preview and the selected building, turning red when the radius is empty. **Manual vegetation clearing**: the Phase 31 bulldozer now also clears a tree/cactus for `VEGETATION_CLEAR_COST` ($8), refunding 2 logs / 1 agave juice — until now `removeVegetation` was only ever reached by depletion, so a tile blocked by a species you had no harvester for was a genuine dead end. **Staffing visibility**: an understaffed/upkeep-unpaid badge on the map sprite (unpaid outranks understaffed, since fixing staffing first would achieve nothing) and `getIdlePopulation()` — computed every tick since Phase 12 and never read by anything — surfaced in the HUD header when non-zero. |
| 2026-09-06 | Material Build Costs | `BuildingDefinition` gained an optional `materials?: Partial<Record<ResourceKey, number>>` (`buildingConfig.ts`) alongside the existing money `cost`, so every prior money-only definition stays valid untouched. Ten walled production/commerce/military buildings (Butcher, Sewery, Warehouse, Supermarket, Barracks, Liquor Still, Saloon, Horsery, Bank, Cactus Milker) now cost 5-10 Wood, creating real downstream demand for the WoodCutter chain; WoodCutter itself costs 5 Logs rather than Wood (costing it Wood would be an unbreakable chicken-and-egg lock, since it's the only building that turns Logs into Wood); Fence costs 2 Logs; Road and every core early building (House/Well/CattleFarm/ChickenFarm/PigFarm/CowRanch/Forestry/PotatoField) stay material-free so the opening game is never gated on a chain that doesn't exist yet. `gameState.ts` gained `hasEnoughMaterials`/`describeMissingMaterials`, folded into `canAfford` (now money AND materials) and `getPlacementRejection` (distinct "Not enough money"/"Not enough materials: need ..." messages); `placeBuilding` deducts `definition.materials` from the global resource pool alongside money and emits `resources-changed`. `buildingConfig.ts`'s `formatResourceMap` was exported and a new `formatBuildingCost` ("$80" or "$80 + 5 Wood") backs `describeBuilding`'s tooltip line and `BuildingBar`'s cost tag; `BuildingBar` also now listens for `resources-changed` (not just `money-changed`) to re-dim buttons that are unaffordable on materials alone. |
| 2026-09-06 | Watchtower & Palisade | Two Tier-1 defense features, both reusing existing combat/HP plumbing rather than adding new systems. **Watchtower**: new 1x1 staffed `BuildingType.Watchtower` in `buildingConfig.ts` (cost 130 + 6 Wood, upkeep 1, `requiresWorkers: true`, no production, maxHp 60, `BuildingCategory.Military` so it lands in the existing tab next to Barracks/Horsery) with its own `WATCHTOWER_SPRITE` (tall lookout-cabin-on-stilts silhouette) auto-registered into `BUILDING_ATLAS_KEY`/the building bar via the existing `BUILDING_SPRITES`/`BUILDING_DEFINITIONS` records. `MainScene.resolveWatchtowerFire` runs alongside `resolveCowboyFire` in `runRaidCombatTick`: every staffed, non-destroyed, upkeep-paid Watchtower auto-fires `WATCHTOWER_DAMAGE` (6, new constant) at the nearest raider within `WATCHTOWER_RANGE_TILES` (6) each combat tick, reusing `findNearestRaider`/`spawnCowboyShotVisual`/`playCombatVolley` outright instead of duplicating a second shooter pipeline - a stationary, longer-range but weaker-per-shot complement to a manually positioned Cowboy. `BuildingInfoPanel` gained an `isWatchtower` branch: a `Defense: Active/Inactive (understaffed)` status line (distinct from Barracks/Horsery's `Staffed:` line and the generic `Storage bonus:` fallback, both of which read wrong for an auto-firing building) plus a `Range: 6 tiles | Damage: 6/shot` line. **Palisade**: Fence stops being pure decoration. `MainScene.pickRaiderTarget` still picks the nearest (preferred-type) building exactly as before, but now runs the result through the new `findBlockingFence` - a rough, non-pathfinding straight-line sample (half-tile steps from raider to target, via `getBuildingAtTile`) that returns the first live Fence tile crossed - and redirects the raider to attack that Fence instead. No new state or destruction path was needed: Fences already carry `hp`/`maxHp` from Phase 21 and already die through the shared `removeBuilding`/`destroyBuilding` path any building uses, and `updateRaiderTargeting`'s existing "current target dead or missing -> re-pick" check is what naturally chains a raider through successive Fence layers to its original target with zero extra bookkeeping. |
| 2026-09-06 | Endless Mode & Difficulty Select | New pre-game (and post-game-over) DOM overlay `ui/DifficultySelectOverlay.ts` (same fixed-backdrop pattern as `GameOverOverlay`, new `#difficulty-select-overlay`/`#difficulty-select-panel` CSS in `index.html`) lets the player pick a `Difficulty` (`easy`/`normal`/`hard`, `constants.ts`'s `DIFFICULTY_SETTINGS` bundling `startingMoneyMultiplier`/`upkeepMultiplier`/`raidEscalationMultiplier` - Normal is `{1,1,1}`, reproducing the pre-Phase-39 numbers exactly) and a `RunMode` (`fixed`, unchanged 3-day buzzer, or new `endless`, which repeats the day/night cycle forever and only ever ends via the existing `'destroyed'` reason). Both selections flow into `gameState.resetGame(options?: { mode?: RunMode; difficulty?: Difficulty })` (defaults `fixed`/`normal`, so any hypothetical no-args caller is unaffected) rather than mutating `constants.ts`'s base values, which stay the Normal baseline: `money` is scaled by `startingMoneyMultiplier` on reset, `runUpkeep` scales each building's base upkeep by `upkeepMultiplier` every tick, and `getDayNumberAtElapsed`/`tickTimer` only apply the `DAY_COUNT` ceiling/buzzer in `fixed` mode - in `endless` mode `elapsedSeconds` and the derived day number simply keep counting up forever, so the existing `Day N - Daytime/Night` HUD header (`MainScene.formatTimerText`) needed no change to drop its implicit "of 3" ceiling. `getThreatLevel()`'s time-based half is now mode-aware: `fixed` keeps dividing elapsed seconds by `GAME_DURATION_SECONDS` (scaled by `raidEscalationMultiplier`), while `endless` has no total run length to divide against, so it saturates asymptotically against completed day/night cycles instead (`cycles / (cycles + ENDLESS_THREAT_RAMP_CYCLES)`, new constant = `DAY_COUNT`) rather than hitting the old fixed-mode ceiling once and sitting flat at max threat for the remainder of a potentially unbounded run. `MainScene.pauseForPreGameSelection` (called once at the end of `create()`) reuses `setupGameOverHalt`'s exact `time.timeScale`/`tweens.timeScale` = 0 pause primitive so nothing advances behind the picker; the existing `setupGameReset`'s `'game-reset'` listener (which already restores both scales to `this.gameSpeed`) is what un-pauses it, since `resetGame()` fires that same event - no separate un-pause path needed for either a first run or a restart. `GameOverOverlay`'s Play Again button no longer calls `resetGame()` directly; it emits a new `gameEvents` entry `'request-run-restart'`, which `DifficultySelectOverlay` listens for to re-show itself, so a new run always goes through a fresh difficulty/mode choice instead of silently reusing the last one. |
| 2026-09-06 | Attack Orders, Focus Fire & Unit HP Bars | `CombatUnit` (`MainScene.ts`) gained `attackTargetRaiderId: string \| null`; `Raider` gained a stable `id` (assigned from a new `raiderIdCounter` in `spawnRaider`) and `maxHp` (the wave's `hpMultiplier`-scaled cap, needed for a correct HP-bar ratio on escalated waves). Right-clicking a live raider while units are selected (`findRaiderAt`, `RAIDER_ATTACK_HIT_RADIUS_PX`) now calls `issueUnitAttackOrder` instead of `issueUnitMoveOrders`, locking every selected unit onto that raider's id; a new per-combat-tick pass `resolveUnitAttackOrders` (called from `runRaidCombatTick` before `resolveRaiderAttacks`) drives each ordered unit via the shared `approachOrEngageRaiderTarget` (walk into `COWBOY_RANGE_TILES` if out of range, else stop and hold), clearing `attackTargetRaiderId` once its raider is gone so the unit falls back to `resolveCowboyFire`'s default nearest-in-range rule automatically. `resolveCowboyFire` now resolves its target per-unit through `resolveUnitFireTarget`, which focus-fires the ordered raider (only once actually in range) ahead of the old nearest-raider fallback. A plain move order (`issueUnitMoveOrders`) explicitly clears `attackTargetRaiderId` so a new command always supersedes a standing attack order. Investigated "attack-move": `resolveCowboyFire` already reads each unit's live `image.x/y` (kept current every frame by Phaser's own tween stepping, independent of the 2s combat-tick timer), so units already auto-fire at whatever's nearest-in-range while mid-walk to any move order's destination — no separate attack-move keybind was added since it would duplicate this existing behavior under a new name. Unit HP bars: a new shared `unitHpBarGraphics` Graphics object (same one-object-per-tick-redraw discipline as building HP bars' `hpBarGraphics`, depth `HP_BAR_DEPTH`) is redrawn by `redrawUnitHpBars` on every `'production-tick'` and again directly at the end of `runRaidCombatTick` — one small two-segment bar per live Cowboy/Cowboy-on-Horse (hidden at full HP via `getUnitHp` against `COWBOY_MAX_HP`/`MOUNTED_COWBOY_MAX_HP`, matching the building convention) and one per live raider (always shown, since raiders are transient combat-only entities), via the shared `drawUnitHpBar` helper and new `UNIT_HP_BAR_WIDTH`/`HEIGHT`/`MARGIN_ABOVE_PX`/`UNIT_SPRITE_HALF_HEIGHT_PX` constants. Skipped the optional hover-cursor-feedback nice-to-have per the phase spec's own escape hatch. |
| 2026-09-06 | Hotkeys, WASD Camera & Control Groups | New `MainScene.setupKeyboardCamera` (called once from `create()`) binds W/A/S/D + arrow keys via individual `addKey(KeyCodes.*)` calls (not `addKeys()`, to stay strictly typed without an `any`/`object` cast) into a `cameraKeys` record; `updateKeyboardCameraPan`, driven every frame from `update(_time, delta)` (which had to gain a `delta` param - it was previously `update(): void`), normalizes WASD/arrow diagonal input and applies `CAMERA_KEYBOARD_PAN_SPEED_PX_PER_SEC` (480, new `constants.ts` value) straight onto `scrollX`/`scrollY`, same unscaled-by-zoom convention `setupCameraDrag`'s right-drag pan already uses, so it's clamped by the same `camera.setBounds` for free. All the non-continuous hotkeys live in a second new method, `setupHotkeys`, on one raw `keyboard.on('keydown', ...)` listener (needed for `event.ctrlKey`/`event.code`, which per-key `addKey` state can't give you) rather than scattered `keydown-X` bindings: **control groups** — `CombatUnit` gained a stable `id` (`unit-${unitIdCounter++}`, mirroring `Raider.id`) so a new `controlGroups: Map<number, string[]>` can survive `cowboyUnits` churn; Ctrl+1..9 (`handleNumberKey`) snapshots `selectedUnits` ids into a group (no-op if nothing selected), bare 1..9 recalls it (filtered live through the existing `isCowboyUnitAlive`, so dead members just silently drop out) and a second recall of the *same* group within `CONTROL_GROUP_DOUBLE_TAP_MS` (400ms) additionally recenters the camera via `centerCameraOnUnits`. **Double-click select-all-of-kind**: `selectUnitAt` now tracks `lastUnitClickId`/`lastUnitClickAt`; a second click-select on the same unit within `UNIT_DOUBLE_CLICK_MS` (300ms) selects every currently-alive unit sharing its `kind` (cowboy vs cowboyOnHorse) instead of just the one hit. **Idle-unit cycling**: Space (`cycleIdleUnitSelection`) filters `cowboyUnits` to those with `moveTween === null && attackTargetRaiderId === null`, advances from the currently-selected unit's index in that list (or starts at 0), wraps via modulo, and centers the camera on the pick; no-op if none are idle. **Delete/Backspace** (`demolishSelectedBuilding`) calls the same `gameState.demolishBuilding` + `'building-selected'`-null-emit pair `BuildingInfoPanel`'s own Demolish button already used, against `MainScene`'s existing `selectedBuildingId`; no-op if nothing is selected. **Building-category number keys**: a new `gameEvents` entry `'select-category': (category: BuildingCategory) => void` lets `trySwitchBuildingCategory` (1-indexed against `Object.values(BuildingCategory)`, the same order `BuildingBar` already builds its tabs in) ask `BuildingBar` to call its existing-but-previously-unreachable-from-outside `setCategory`. **Bare-digit conflict resolution** (both features want keys 1-9): `handleNumberKey` always tries a control-group recall first; only when that group has no living members (never assigned, or every member since died) does the digit fall through to `trySwitchBuildingCategory`, and only then if `selectedUnits` is empty and neither placement (`selectedType`) nor `demolishMode` is active — an assigned, still-alive group therefore permanently "owns" its digit over the category tab sharing it. |
| 2026-09-06 | Worker Priority & Staffing Control | `assignWorkforce` (`gameState.ts`) no longer staffs `placedBuildings` in raw placement order: each `PlacedBuilding` gained a `priority: WorkerPriority` field (`'high' \| 'normal' \| 'low'`, new type in `buildingConfig.ts`, defaulted to `'normal'` in `placeBuilding`), and the greedy assignment pass now walks a `[...placedBuildings].sort()` by `WORKER_PRIORITY_RANK` (High, then Normal, then Low) before handing out the shared population pool - the sort is stable, so placement order remains the tiebreak within a tier exactly as before, and `placedBuildings` itself is untouched. New `gameState.setBuildingPriority(buildingId, priority)` mutates the field only; the next tick's `assignWorkforce` picks it up with nothing else to recompute. `assignWorkforce` also now tracks total worker `demand` across every priority-eligible building and exposes `laborShortfall = max(0, demand - totalPopulation)` via new `getLaborShortfall()` - distinct from the existing `getIdlePopulation()` (spare workers, no job) in that this is jobs with no worker. `BuildingInfoPanel.ts` gained a `renderPriorityControls` row (High/Normal/Low buttons, `.priority-button`/`.active` CSS in `index.html`) shown for any building with `workersRequired > 0`, plus a shared `formatUnderstaffedReason(building, workersRequired)` helper - returns "not enough population (need N more workers town-wide)" once `getLaborShortfall() > 0`, else falls back to the prior "understaffed (X/Y workers)" wording - now threaded through `describeHarvestStatus`'s understaffed branch, the Barracks/Horsery/Bank/Watchtower `statusText` branches, `formatSaleText` (Supermarket/Saloon, signature changed to take the building + workersRequired instead of a bare `staffed` boolean), and a new dedicated `Understaffed: ...` line for plain (non-harvest) production buildings that were previously just a silent "Production: Off". `ResourceHudPanel.refresh()`'s header line appends `(short by N)` next to `Pop employed/total` whenever `getLaborShortfall() > 0`. |
| 2026-09-06 | Drag-to-Place Lines (Road/Fence/Wall) | New `buildingConfig.ts` `LINE_PLACEMENT_BUILDING_TYPES`/`isLinePlacementBuilding` marks just Road and Fence as line-friendly (deliberately not every 1x1 building - House/Well/Watchtower stay meaningfully singular placements). **Left-drag conflict with Phase 25's unit box-select**: both features want the exact same left-drag gesture, resolved purely by branching on existing state rather than adding a new mode flag - `setupCameraDrag`'s `pointermove` now computes `isLineDragging` (`selectedType !== null && isLinePlacementBuilding(selectedType) && leftButtonDown() && dragDistance > CLICK_MOVE_THRESHOLD`) *before* its pan/box-select/line branches; box-select's own branch is unchanged and already required `selectedType === null`, so the two conditions are mutually exclusive by construction and box-select needed zero code changes. When `isLineDragging`, `updateLinePreview` renders every tile of a `computeLineTiles(startX,startY,endX,endY)` dominant-axis-first L-shaped run (horizontal-or-vertical leg first per whichever `|dx|`/`|dy|` is larger, then a perpendicular leg to the end point) via a pooled, grow-only `linePreviewImages[]` array (one Image per tile index, `setTexture`-refreshed rather than destroyed/recreated), each tile tinted green/red per `getPlacementRejection` exactly like the existing single-tile preview, plus a `lineCostText` tag ("N/total Label - $X + Y Wood") summing `cost`/`materials` over currently-valid tiles only. Since a zero-length drag degenerates `computeLineTiles` to exactly one tile, placement for line-friendly types was moved from `pointerdown` to a new `pointerup` handler (`commitLinePlacement`) shared by plain clicks and real drags alike; non-line-friendly types are unaffected and still place immediately on `pointerdown` via the same `tryPlaceAt`, now backed by a shared `placeBuildingAt(tileX, tileY): boolean` extracted from its old body (returns success instead of void, and does NOT play the placement sound itself) so `commitLinePlacement` can call it once per tile in sequence - each call re-runs `placeBuilding`'s own `canPlaceBuilding` check against the *current* money/materials, so a line that outruns the player's funds just silently stops placing partway through rather than overspending or aborting the whole line; the sound plays once per commit (single tile or whole line) instead of once per tile. **Shift-to-repeat**: previously placement stayed active forever until an explicit Escape/right-click (`cancelPlacement`), so a single House click had no non-Escape way to end placement mode - flipped via new `applyShiftRepeatPolicy` (checked after every successful placement, single or line): holding Shift (`shiftKey`, a plain `addKey(SHIFT)`) now keeps the tool selected for the next placement, while a plain click exits immediately - implemented as `gameEvents.emit('cancel-placement')` rather than calling `this.cancelPlacement()` straight, since `BuildingBar`'s active-button highlight is itself only driven by that event and would otherwise silently desync from the scene's actual state. `cancelPlacement`/game-reset additionally hide the line-preview pool via new `hideLinePreview()`. No changes needed to Phase 41's hotkeys/control-groups (already gated on `selectedType === null`) or ESC-cancel. |

| 2026-09-06 | Notification Log & Alert System | New `state/notifications.ts` (`NotificationEntry { id, message, tickElapsedSeconds, kind: 'info' \| 'warning' \| 'danger', buildingId? }`, `addNotification`/`getNotifications`/`clearNotifications`, capped at `MAX_NOTIFICATION_LOG_ENTRIES` 50) is the single log, kept standalone like `state/vegetation.ts` rather than folded into `gameState.ts` - it never imports gameState, so every caller passes `elapsedSeconds` explicitly. A new `gameEvents` entry `'notification-added'` fires on every append; `'camera-focus-requested'` lets the DOM panel ask `MainScene` to pan without owning any camera/tile math itself. Real triggers, each firing once per occurrence via small debounce state: **destroyed buildings** (`gameState.removeBuilding`, `reason === 'destroyed'` only, danger); **raid incoming** (`MainScene.showRaidNotice` now also calls `addNotification` with the exact banner text - the temporary `raidNoticeText` HUD banner is untouched, this is additive, danger); **stalled production chains** (new `runProductionTick` block tracks `stalledInputTicks`/`stalledInputNotified` Maps/Sets keyed by buildingId, firing once a staffed/enabled/non-harvest building with declared `production.inputs` has been missing one for `PRODUCTION_STALL_NOTIFY_TICKS` (3) consecutive ticks, resetting the moment it recovers so a later stall can refire, warning); **storage-cap waste** (the output-application loop in `runProductionTick` already computed `after - before` against `storageCap` but silently discarded the remainder - now `wasted = produced - (after - before)` debounces per `ResourceKey` via `resourcesWastingAtCap`, a global-to-the-pool Set rather than per-building since the cap itself is global, with a recovery pass after `runSaloonSales()` clearing any key that's dropped back below cap, warning); **upkeep-unpaid** (`runUpkeep`'s existing `building.disabled = true` branch gained an `upkeepDisabledNotified` Set for the same fire-once-on-transition shape, warning). All four debounce structures are cleared in `removeBuilding` (per-building ones, keyed on the dying id) and `resetGame` (`clearNotificationDebounceState` + `notifications.clearNotifications()`). Skipped the House-needs/tier trigger from the roadmap item - Population Needs & House Tiers hasn't landed yet as of Phase 43, so there is no such state to hook into. UI: new DOM `ui/NotificationLogPanel.ts` (matches `BuildingInfoPanel`'s DOM choice, not `ResourceHudPanel`'s Phaser-canvas one), anchored bottom-left in `#stage` opposite `BuildingInfoPanel`'s bottom-right, collapsible via a clickable header, showing the last 20 entries newest-first color-coded by kind; a clicked entry with a `buildingId` computes that building's world-center from `BUILDING_DEFINITIONS` size + `TILE_SIZE` and emits `'camera-focus-requested'` (silently no-ops if the building's gone). `MainScene.centerCameraOnWorldPoint` extracted from `centerCameraOnUnits`' own centerOn-then-redraw-minimap-viewport pair so the panel's single-point pan and the existing multi-unit centering share one implementation. |
| 2026-09-06 | Minimap Combat Awareness | New `MainScene.minimapCombatGraphics` (created in `setupMinimap`, depth 1002, above `minimapViewportGraphics`) is redrawn by `redrawMinimapCombat`/`redrawMinimapCombatThrottled` every `update()` frame at the exact `MINIMAP_VIEWPORT_THROTTLE_MS` (50) interval the viewport rectangle already uses (own `lastMinimapCombatRedraw` timestamp) rather than only on placement/camera events, since units and raiders move continuously - it draws a `MINIMAP_UNIT_COLOR` (blue) dot per live `cowboyUnits` entry and a `MINIMAP_RAIDER_COLOR` (red) dot per live `raiders` entry straight off each unit's own `image.x/y`. Building damage flashes: `resolveRaiderAttacks` now calls `registerMinimapBuildingDamage(target)` right after mutating `target.hp`, snapshotting the hit building's centre tile into a new `minimapBuildingFlashes: Map<string, MinimapBuildingFlash>` (`{tileX, tileY, until}`) so `redrawMinimapBuildingFlashes` can alternate the dot between `MINIMAP_BUILDING_DOT_SIZE` and a larger `MINIMAP_FLASH_DOT_SIZE` every `MINIMAP_FLASH_BLINK_MS` (200) for `MINIMAP_FLASH_DURATION_MS` (1.6s) - keyed by a position snapshot rather than a `PlacedBuilding` reference so a killing blow's flash survives `removeDestroyedBuildings()` dropping the record the same tick. Off-screen attack ping: the same damage hook checks the hit building's world position against `cameras.main.worldView` (new `isWorldPointInViewport`) and, if it's outside, upserts a new `offscreenThreats: Map<string, OffscreenThreat>` (`{worldX, worldY, lastHitAt}`); `redrawOffscreenThreatPings` re-checks on-screen-ness every redraw (so panning onto the fight clears it immediately), otherwise draws a pulsing `OFFSCREEN_PING_COLOR` dot at the point where a line from the minimap's centre to the threat's minimap position crosses the minimap's own border (inset by `OFFSCREEN_PING_EDGE_INSET`), fading out over `OFFSCREEN_PING_FADE_MS` (3s) since the last hit. Both maps are cleared in `resetRaidState` (covers `game-reset` too). All new draws are plain `Graphics` fills on a non-interactive object, so the existing minimap click/drag-to-navigate (`isPointerInMinimap`/`minimapPointerActive`) is untouched. |
| 2026-09-06 | Population Needs & House Tiers | The Tier-1 roadmap item: Houses now consume resources and grow through 3 tiers instead of granting a flat `POPULATION_PER_HOUSE`. `buildingConfig.ts` gains `HouseTier` (`1 \| 2 \| 3`), `HouseNeedGroup` (`{ label, options: Partial<Record<ResourceKey, number>> }` - an "or" group, e.g. Meat-or-Eggs, tried in declared key order and consuming the first affordable option) and `HOUSE_TIER_CONFIG: Record<HouseTier, HouseTierConfig>` with the chosen needs/population/tax table: **Tier 1** `{ needs: [Water 0.2], population: POPULATION_PER_HOUSE (2), tax: $0 }`; **Tier 2** `{ needs: [Water 0.3, Meat-or-Eggs 0.1], population: 4, tax: $2/tick }`; **Tier 3** `{ needs: [Water 0.4, Meat-or-Eggs 0.15, Clothes-or-Liquor(0.05/0.1)], population: 6, tax: $5/tick }` - sized against real production rates (e.g. one fully-stocked Chicken Farm's 0.8 eggs/tick alone covers 8 Tier-2/3 houses' Meat-or-Eggs need). `PlacedBuilding` gained `houseTier` (defaults 1 on placement), `houseNeedsMetStreak`/`houseNeedsUnmetStreak` (hysteresis counters) and `houseNeedsStatus` (last tick's per-group met/missing snapshot for the UI). New `gameState.runHouseNeeds()` runs after `runSupermarketSales`/`runSaloonSales` in `runProductionTick` (Houses are end-of-chain consumers, so this position lets a House spend goods finished earlier in the same tick) and is atomic per House per tick - either every need group finds an affordable option and all get consumed together (plus tax collected), or nothing is consumed at all; a fully-met tick increments `houseNeedsMetStreak`/zeroes the other, an unmet tick the reverse. Crossing new constant `HOUSE_TIER_HYSTERESIS_TICKS` (5, `constants.ts`) consecutive ticks in either direction changes the tier by exactly one and resets both streaks; disabled (upkeep-unpaid) or 0-HP Houses are skipped entirely (idle, not penalized further), matching every other pass's disabled-building semantics. `assignWorkforce`'s `totalPopulation` is now `sum(HOUSE_TIER_CONFIG[house.houseTier].population)` over every House instead of `houseCount * POPULATION_PER_HOUSE`, so the HUD's `Pop employed/total` (`ResourceHudPanel`, unchanged code) picks up tiered population automatically. New `gameEvents` entry `'house-tier-changed': { building, direction: 'upgrade' \| 'downgrade' }`; `addNotification` fires an `info`/`warning` log entry on every flip (already debounced by the hysteresis counters, so no separate notification debounce state needed - closes the "skipped" item from Phase 44's log). Visuals: two new `BootScene.ts` sprites (`HOUSE_TIER2_SPRITE` - painted trim stripe + second full window row; `HOUSE_TIER3_SPRITE` - flagged double parapet, twin windows, gold balcony rail replacing the door) get their own atlas frames via `buildingTextureKey(type, tier?)` (extended to accept an optional `HouseTier`, a no-op for every non-House type) and `generateBuildingAtlas`'s layout gains two extra appended frames for them; `MainScene.setupHouseTierVisuals` listens for `'house-tier-changed'`, swaps the building's `Image` texture via the same helper, and plays a small non-audio cue (scale-pop tween on upgrade, brief red tint flash on downgrade). `BuildingInfoPanel` gained `formatHouseTierText`/`formatHouseNeedsText`/`formatHouseProgressText` showing "Tier X/3 \| Population: +N \| Tax: +$N/tick", "Needs: Water OK, Meat or Eggs MISSING", and an upgrade-in-N-ticks/downgrade-risk-in-N-ticks countdown. Decorative wandering villager sprite count (`spawnVillagersForHouse`) is deliberately left un-scaled by tier - it was always a capped cosmetic flourish decoupled from the real (now tiered) population figure, and stays that way. |
| 2026-09-06 | Milestone-Gated Building Unlocks | The other Tier-1 roadmap item: not all 22 buildings are available from minute zero anymore. `buildingConfig.ts` gains `UnlockRequirement` (`{ populationAtLeast?, netWorthAtLeast?, dayAtLeast? }`, all-present-fields-AND'd) and an optional `unlockRequirement` on `BuildingDefinition`; undefined = always unlocked. Six core buildings stay unlockRequirement-less (House, Well, Road, Fence, CattleFarm, ChickenFarm). Chosen table - **Wave 1** (`populationAtLeast` 4-6, basic chain): Butcher 4, PotatoField 4, PigFarm 5, CowRanch 6. **Wave 2** (moderate population/net-worth/day-2, mid-tier): Forestry 6, WoodCutter 8, Sewery 8 (population), Warehouse $2200, Supermarket $2500 (net worth), Barracks day 2. **Wave 3** (population 10+/day 2-3/net-worth, luxury/military): Liquor 10, CactusMilker 12 (population), Saloon $3200, Bank $4000 (net worth), Horsery day 3, Watchtower day 2 + population 10 (combined). Net-worth thresholds were deliberately set well above `STARTING_MONEY` (1800, `constants.ts`) since `computeNetWorth().total` includes cash on hand - a threshold below that would be satisfied at t=0 and gate nothing. New `gameState.isBuildingUnlocked(type)` reads whichever fields the requirement names (`totalPopulation`, `getDayNumber()`, `computeNetWorth().total`, each only computed if referenced) and `describeUnlockRequirement(type)` renders them as "Unlocks at Population 10" / "Unlocks at Day 2, Net Worth $2200" for tooltips. `getPlacementRejection` checks unlock first (before terrain/afford checks) as a defense-in-depth backstop, though the real gate is UI-side. `BuildingBar.ts`'s `refreshButtonStates` (renamed from `refreshAffordability`) now toggles a `locked` class distinct from `unaffordable` (greyscale + a lock-emoji badge via `#building-bar button.building-button.locked`/`::after` in `index.html`, `unaffordable` suppressed while locked) and swaps the button's tooltip to `describeUnlockRequirement`'s text; `onButtonClick` returns immediately for a locked type (no `select-building` emit, no placement mode) while an unlocked-but-unaffordable button is untouched from Phase 33's existing behavior. Recomputed on `resources-changed`, `production-tick`, `house-tier-changed` and `building-placed` (population/net-worth/day all drift or jump on these) rather than every frame. New `gameState.checkBuildingUnlocks()` (called at the end of `runProductionTick` and `placeBuilding`) fires a debounced info-kind `addNotification` ("New building unlocked: Warehouse") the instant a still-locked building's requirement is first met, tracked in a module-level `unlockNotified: Set<BuildingType>` cleared in `resetGame` alongside the other notification debounce state. |
| 2026-09-06 | Chain Encyclopedia & Resource Tooltips | New `config/resourceGraph.ts` derives a static producer/consumer lookup by scanning `BUILDING_DEFINITIONS` on every call (cheap at ~20 types, so no cache to go stale): `getResourceProducers`/`getResourceConsumers` check `production.outputs`/`production.inputs` plus, critically, `harvest.outputs` and `animal.outputPerAnimal` (CattleFarm/PigFarm/CowRanch/ChickenFarm all declare an empty `production: {}` and make everything through an `AnimalConfig` - a lookup that skipped it would report zero producers for rawMeat/leather/eggs) and, on the consumer side, House's `HOUSE_TIER_CONFIG` need groups plus the `SUPERMARKET_SELL_RATES`/`SALOON_SELL_RATES` tables (a sold resource leaves the pool exactly like a production input, just via `runSupermarketSales`/`runSaloonSales`); `getResourceChainBuildingTypes` unions both into the one Set the map overlay needs. `ResourceHudPanel.ts` gets an invisible `Phaser.GameObjects.Zone` per resource row (more forgiving to hover than the icon Image alone) driving a small new DOM tooltip (`#resource-tooltip` in `index.html`, styled to match `#building-info-panel` - a Phaser canvas widget can't reuse the building bar's native `title`-attribute trick) showing current stock, `getResourceTrends()`'s net rate, and the producer/consumer building labels; the tooltip's numbers refresh live off the panel's existing `refresh()` rather than freezing at hover-start. Clicking a row's zone emits a new `gameEvents` entry `'resource-selected': (key: ResourceKey \| null) => void` (toggling off on a second click of the same key), which `ResourceHudPanel` itself listens for (so an external clear stays in sync with its own gold row-outline highlight, drawn into the existing `background` Graphics rather than a new object) and which `MainScene.setupChainView` also listens for to redraw a new shared `chainViewGraphics` (depth 21, same one-Graphics-object-per-tick-redraw discipline as `connectionGraphics`) - a gold rect outline over every currently-placed building whose type is in the selected resource's chain, refreshed on `'connections-updated'` (already fired on every placement/removal regardless of type) so a newly-built matching building lights up for free. Escape clears the selection (`keydown-ESC`, mirroring the existing placement-cancel binding); a new **'C'** hotkey (unbound as of Phase 41, added to `setupHotkeys`' single `keydown` listener) toggles the overlay's visibility without discarding the selection - distinct from Escape/re-click, which drop it outright. `game-reset` clears `chainViewGraphics`, resets `chainViewVisible` to true and emits `resource-selected(null)` so a fresh run never inherits a stale highlight. Read-only/informational throughout - no production-tick or gameplay logic touched. |

| 2026-09-06 | Statistics & Efficiency Panel | The other Tier-1 roadmap item: an opt-in, off-by-default "what's actually happening" window, toggled by a new **'V'** hotkey (`MainScene.setupHotkeys`, added alongside Phase 48's 'C') or the building bar's new **Stats** button (`BuildingBar.createStatsButton`) - both just emit a new `gameEvents` entry `'toggle-statistics-panel'`, which `ui/StatisticsPanel.ts` is the sole listener for, owning all shown/hidden state itself. `gameState.ts` gained two new read-only tracking layers, both purely observational (no production/staffing/upkeep branch or value was changed, only annotated): **(1)** a rolling `RESOURCE_HISTORY_LENGTH` (60) -entry ring buffer per `ResourceKey` (`resourceHistory`, new `ResourceHistoryEntry { produced, consumed, net }`) built from per-tick `tickResourceProduced`/`tickResourceConsumed` accumulators (`addProducedThisTick`/`addConsumedThisTick`) threaded into every spot that already mutates `resources` - the main production/harvest output loop and input-deduction loop, `runSupermarketSales`, `runSaloonSales`, and `runHouseNeeds`'s consumption loop - reset at the top of `runProductionTick` and snapshotted via `pushResourceHistorySnapshot()` right after `resourceTrends` (Phase 33) is recomputed, so `net` can never disagree with the HUD's own trend readout; exposed via `getResourceHistory(key)`. **(2)** a per-building rolling productivity window (`PRODUCTIVITY_WINDOW_TICKS` 20 booleans, side `productivityRecords: Map<buildingId, ProductivityRecord>` - kept off `PlacedBuilding` itself like Phase 44's debounce Maps, cleaned up in `removeBuilding` and `resetGame`) written by a new `recordProductivityTick(buildingId, active, reason)` call at every existing early-`continue`/success point in `runProductionTick`'s per-building loop - reusing the exact blocker-priority order Phase 35's `describeHarvestStatus` established (destroyed → understaffed → upkeep unpaid → no animals/no vegetation in range/missing inputs → running); only buildings with a `production` or `harvest` config are tracked (Road/House/Warehouse/Bank/Barracks/Horsery/Watchtower/Supermarket/Saloon have no on/off production state to describe and are skipped), exposed via `getBuildingProductivity(buildingId): { activeTicks, totalTicks, blockReason } | null`. `ui/StatisticsPanel.ts` (new DOM overlay, same dark-panel pattern as `BuildingInfoPanel`/`NotificationLogPanel`, anchored top-right so it collides with neither) renders two sections only while visible (gated in its `'production-tick'` listener so a hidden panel costs nothing): a per-resource row with a lightweight inline `<canvas>` sparkline (`drawSparkline`, hand-rolled 2D-context line plot of the tracked window's net rate against a zero baseline, no charting library) plus that tick's "+produced/-consumed (net ±X)" text; and a click-to-sort (`toggleSort`, asc/desc by active-ticks ratio) list of every tracked placed building's `activeTicks/totalTicks` percentage (color-banded good/ok/bad) and current block reason. |
| 2026-09-06 | Stone/Iron -> Blacksmith Tools Chain | The Tier-1 roadmap item, the sixth production chain following Forestry->WoodCutter's exact shape: two flat raw producers feeding one processor, plus a new build material and sellable good closing the loop. Three new `ResourceKey`s (`stone`, `iron`, `tools`; `RESOURCE_VALUES` 3/6/20, `RESOURCE_LABELS` Stone/Iron/Tools) threaded through `Resources`/`emptyResources()` in `gameState.ts`. Three new staffed 2x2 `BUILDING_DEFINITIONS` entries: `Quarry` (cost 110, maxHp 80, `BuildingCategory.Farming` alongside Forestry/PotatoField, material-free, `production.outputs: { stone: 1.2 }`, `unlockRequirement: { populationAtLeast: 8 }`), `IronMine` (cost 160, maxHp 80, same category, `production.outputs: { iron: 0.6 }` - half Quarry's rate, the rarer/slower half of the pair, `unlockRequirement: { populationAtLeast: 10 }`), and `Blacksmith` (cost 180, maxHp 80, `BuildingCategory.Industry` alongside Sewery/WoodCutter/Liquor, `materials: { wood: 6 }`, `production: { inputs: { stone: 2, iron: 1 }, outputs: { tools: 1 } }`, `unlockRequirement: { populationAtLeast: 12 }`) - all three are plain production buildings with no `animal`/`harvest` config, so `runProductionTick`, `getWorkersRequired`, `resourceGraph.ts`'s producer/consumer scan, and `BuildingInfoPanel`'s generic Consumes/Produces rendering needed zero changes to pick them up. Quarry/Iron Mine are hard-gated on Gravel at placement, mirroring Phase 30's Well/water gate exactly: `mapConfig.ts`'s water-only `distanceToNearestWater` was generalized into `distanceToNearestTileType(tileType, ...)` (a thin `distanceToNearestWater` wrapper now calls it, unchanged behavior/callers) so `gameState.ts`'s new `getGravelDistance(tileX, tileY, type)` - using new `GRAVEL_MAX_DISTANCE_TILES` (2, `constants.ts`) - could reuse the exact ring-search rather than duplicate it; `getPlacementRejection` gained the matching hard-gate branch (`Quarry`/`IronMine` must resolve a non-null `getGravelDistance`), and `BuildingInfoPanel`/`describeBuilding` both surface it with the same "must be on/within N tiles" + live-distance-readout pattern the Well already has (no output distance-scaling, unlike Well's `WELL_OUTPUT_BY_DISTANCE` - this is a pass/fail gate only). Tools closes the loop two ways: as a new build material added *alongside* (not replacing) the existing Wood cost on five Wave-2/3 buildings - Warehouse (+2 Tools), Bank (+3), Saloon (+3), Horsery (+4), Watchtower (+2); and as a new sellable good, widening `SupermarketSellableKey`/`SUPERMARKET_SELL_RATES` (`tools: { amount: 2, price: 20 }`) per Phase 26's "widen the union, don't hardcode" convention - `AutoSale<K>`/`runSupermarketSales`/`formatSaleText` needed no changes. New pixel-art frames in `BootScene.ts`: `QUARRY_SPRITE` (walls-free rock-face pit + stone pile + leaning pickaxe, same open-air idiom as `FORESTRY_SPRITE`), `IRON_MINE_SPRITE` (dark mine-shaft entrance with visible ore in the wall window, ore-cart rail yard - `WOOD_CUTTER_SPRITE`'s walled skeleton), `BLACKSMITH_SPRITE` (anvil-in-window + glowing forge - literally `BUTCHER_SPRITE`'s skeleton with the cleaver/counter swapped for an anvil/forge); plus three new 6x6 `RESOURCE_ICON_SPRITES` entries (grey rock-chunk, rust-orange ore-chunk, hammer silhouette). `ResourceHudPanel.ts` needed no layout changes - Phase 33 already derives its row/column layout from `Object.keys(RESOURCE_LABELS).length`, so the panel simply grew three more rows automatically. `resourceGraph.ts`'s producer/consumer scan is fully generic over `BUILDING_DEFINITIONS`/`SUPERMARKET_SELL_RATES`, so the new chain appears in the Chain Encyclopedia/tooltips with no changes there either. |
| 2026-09-06 | Trading Post & Fluctuating Prices | The last Tier-1 roadmap item. New standalone `state/market.ts` (same no-gameState-import shape as `vegetation.ts`/`notifications.ts`) tracks a fluctuating price per `MarketableResourceKey` (`buildingConfig.ts`'s new `SupermarketSellableKey \| SaloonSellableKey` union, plus `MARKETABLE_RESOURCE_KEYS`/`BASE_MARKET_PRICES` derived by reducing `SUPERMARKET_SELL_RATES`/`SALOON_SELL_RATES`' existing `price` fields - those tables are now a fluctuating baseline *peg*, not the sale price itself). Two layers compose `getCurrentMarketPrice(key)`: a slow per-tick random-walk `baselinePrice` (±`MARKET_DRIFT_MAX_FRACTION` 2%, clamped to `MARKET_PRICE_FLOOR_FRACTION`/`CEIL_FRACTION` 50%-150% of the peg) and a temporary supply-side `pressure` penalty (`recordMarketSaleVolume` accumulates each sale into a `MARKET_PRESSURE_WINDOW_TICKS` (10) rolling window, same shape as Phase 49's productivity tracking; summed volume x `MARKET_PRESSURE_PER_UNIT` capped at `MARKET_MAX_PRESSURE_FRACTION` 60%), floored at `MARKET_ABSOLUTE_FLOOR_FRACTION` (20%) of the peg so a price can never crash to zero. `runMarketTick(elapsedSeconds)` runs once at the top of `gameState.runProductionTick` (before any sell pass) - deliberately a one-tick lag, so this tick's sales read a price set by *last* tick's volume rather than discounting themselves mid-tick. `runSupermarketSales`/`runSaloonSales` were changed minimally: `rate.price` (constant) became `getCurrentMarketPrice(key)` (live) plus a `recordMarketSaleVolume(key, soldAmount)` call each - their rate tables, `AutoSale<K>` shape, and `BuildingInfoPanel.formatSaleText` needed no other changes. New staffed 2x2 `BuildingType.TradingPost` (`BUILDING_DEFINITIONS`, cost 260 + 10 Wood/4 Tools, `BuildingCategory.Commerce`, upkeep 2, `unlockRequirement: { netWorthAtLeast: 5000, dayAtLeast: 3 }` - above Bank's 4000, the latest-gated Commerce building) has no `production` field; instead each `PlacedBuilding` carries `tradeOrders: Partial<Record<MarketableResourceKey, TradeOrderConfig>>` (`{ enabled, threshold, amount }` per resource, defaulting via new `TRADING_POST_DEFAULT_THRESHOLD`/`AMOUNT`), set one resource at a time via `gameState.setTradingPostOrder(buildingId, key, update)`. New `gameState.runTradingPostSales()` (called after `runSaloonSales`, before `runHouseNeeds`) sells up to `amount` of any `enabled` order whose stock exceeds `threshold`, at the same `getCurrentMarketPrice`/`recordMarketSaleVolume` calls the other two sell passes use - all three autonomous/manual sellers draw down and depress the *same* market, not three independent ones. `BuildingInfoPanel.ts` gained `renderTradeOrderRows`/`buildTradeOrderRow`: one row per `MARKETABLE_RESOURCE_KEYS` entry with a priority-button-styled enable toggle and two `input[type=number]` fields (`.trade-order-row`/`.trade-order-input` CSS in `index.html`) bound on `'change'` (not `'input'`, to avoid stealing focus on the panel's existing full-`render()`-per-action pattern). Traveling Merchant: `MainScene.setupMerchantSystem`/`scheduleNextMerchantCheck` is a direct copy of `scheduleNextRaidCheck`'s self-rescheduling shape (random `MERCHANT_MIN_INTERVAL_MS`-`MERCHANT_MAX_INTERVAL_MS` (90-180s) delay, fire, reschedule) minus any wave/eligibility gating; `triggerMerchantDeal` picks a random `MarketableResourceKey` and a `MERCHANT_MULTIPLIER_MIN`-`MAX` (1.5x-2x) spike for `MERCHANT_DEAL_MIN`-`MAX_SECONDS` (30-60s) via `market.startMerchantDeal`, and announces it through Phase 44's `addNotification` (info kind) - no bespoke UI, the HUD tooltip already reflects the live price. `resetMerchantState`/`resetMarket` (called from `game-reset`/`gameState.resetGame` respectively) mirror `resetRaidState`'s cancel-and-reschedule-fresh discipline so a new run never inherits a drifted price or a pending merchant countdown. `ResourceHudPanel.renderTooltip` gained a `formatMarketPriceLine` (only for `isMarketable(key)`) showing the live price plus an up/down arrow against `getBaselineMarketPrice` (`market.getBaselineMarketPrice`, the drifting peg without this tick's pressure/deal folded in). `resourceGraph.ts`'s `getResourceConsumers` lists `TradingPost` for every `MARKETABLE_RESOURCE_KEYS` entry (capability, like Supermarket/Saloon, regardless of whether an order is currently configured). New `TRADING_POST_SPRITE` in `BootScene.ts` (open-sided market stall - candy-striped tent roof, hanging balance scale, counter crates/sacks - same open-air idiom as Forestry/Cactus Milker). This closes out all 15 Tier-1 roadmap phases (37-51). |

| 2026-09-06 | Save / Load + Autosave | New `src/state/persistence.ts`: `serializeGameState()`/`deserializeGameState(save)` round-trip a versioned (`version: 1`) `SaveGameV1` payload through `localStorage` under `western-village-save-<slot>` (`saveToSlot`/`loadFromSlot`/`deleteSlot`/`hasSaveSlot`/`listSaveSlots`/`getMostRecentSaveSlotName`), with `manual` and `autosave` slots. **Persisted**: money, the resource pool, `elapsedSeconds` (day/phase are derived from it), difficulty, run mode, total meat produced, every `PlacedBuilding` verbatim (hp, animals, bank balance, garrisoned Cowboy/Cowboy-on-Horse counts+HP, house tier, trade orders, etc.), every live vegetation entity, and the full fluctuating-market state (baseline/current price, pressure volume window, pending volume, active merchant deal) via new `market.ts`/`vegetation.ts` `serialize*`/`restore*` pairs. **Explicitly NOT persisted** (documented scope limit): Phase 49's `resourceHistory` sparkline buffers and the notification log both reset to empty on load (cosmetic); all MainScene-local ephemeral combat state - live unit x/y, in-flight move tweens/orders, control groups, and any raid wave in progress - none of it round-trips, so a load resumes the *town*, not a frozen battle. A restored Barracks/Horsery's garrisoned units (real PlacedBuilding fields, hence saved) are re-spawned at their static home slot rather than their last live position. New `gameState.ts` exports back this: `restoreBuilding` (pushes a fully-formed building into occupancy/placedBuildings/buildingsById, skipping every cost/placement-legality check `placeBuilding` runs), `recomputeWorkforceNow` (forces an immediate `assignWorkforce` pass so HUD population/staffing isn't stale for up to one tick after a load), `silentlySyncUnlockNotifications` (seeds `unlockNotified` from the restored town's current `isBuildingUnlocked()` state so a load doesn't replay every already-unlocked building's notification), and `restoreCoreState` (money/resources/elapsedSeconds/totalMeatProduced). A new `'game-loaded'` event (`gameEvents.ts`), fired by `deserializeGameState` once gameState is fully repopulated but before `updateConnections()`, is MainScene's cue to rebuild every building/villager/garrisoned-unit visual from `getPlacedBuildings()` (`restoreBuildingVisual`, reusing `createVisualForBuilding` - the same visual-creation code `placeBuildingAt` was refactored to share, rather than a second parallel path) so the following `connections-updated` redraw has a populated `buildingVisuals` map to draw against. UI: `DifficultySelectOverlay` gained a "Continue" button (shown only when a save exists, loads whichever of manual/autosave is newest, re-rendered on every `request-run-restart` so a save made mid-run is picked up the next time the overlay shows); `BuildingBar`'s top row gained Save/Load buttons against the `manual` slot (Load uses the native `disabled` attribute when no manual save exists). Autosave: MainScene saves to the `autosave` slot once per genuine dawn off the existing `'day-phase-changed'` event (gated on `phase === 'day'`, a dayNumber not already autosaved, and `elapsedSeconds > 0` to exclude `resetGame`'s synthetic Day-1 event) with a small "Autosaved." notification; a one-shot `suppressNextAutosaveCheck` flag stops a load from immediately re-triggering an autosave via `deserializeGameState`'s own HUD-refresh `day-phase-changed` re-emission.

| 2026-09-06 | Rally Points & Training Queue | Tier-2 roadmap item. Barracks/Horsery training is no longer instant: `trainCowboy`/`trainMountedCowboy` (`gameState.ts`) still deduct `COWBOY_TRAIN_COST`/`MOUNTED_COWBOY_TRAIN_COST` on click and still gate on hp/afford/per-building cap, but now push a `TrainingQueueJob { kind, remainingTicks }` (new `buildingConfig.ts` type) onto a new always-present `PlacedBuilding.trainingQueue` array instead of spawning the unit inline - the cap check sums `count + trainingQueue.length` so queued jobs reserve their slot before existing. New `runTrainingQueues()` (called from `runProductionTick` right after `runUpkeep()`) counts down only the FRONT job of each building's queue per tick (classic one-at-a-time queue, gated on the same staffed/not-disabled/hp>0 conditions as any other requiresWorkers production) seeded from new `COWBOY_TRAIN_TICKS` (4) / `MOUNTED_COWBOY_TRAIN_TICKS` (6) constants; a completed job does exactly what the old inline code did (bump `cowboyCount`/`mountedCowboyCount`, push starting HP, emit the existing `'cowboy-trained'`/`'mounted-cowboy-trained'` events MainScene already spawns visuals from) so only the timing moved. New optional `PlacedBuilding.rallyPoint?: { x, y }` plus `gameState.setRallyPoint`/`clearRallyPoint` (gated to Barracks/Horsery, emit new `'rally-point-changed'`); `BuildingInfoPanel`'s new "Set Rally Point" button emits new `'rally-point-mode-changed'` (buildingId | null) to arm a one-shot pick, mirroring how placement/demolish mode already intercept clicks - `MainScene.setupRallyPoints` listens, and its existing unit-control `pointerup` handler checks `rallyPointModeBuildingId` first (before the pre-existing raider-attack/move-order branch) so an armed pick's right-click can never be mistaken for a unit order; a qualifying click (not a right-drag pan) calls `setRallyPoint` and self-disarms. A newly-spawned unit is walked straight to its building's rally point via the existing `issueUnitMoveOrder`, wired only into the `'cowboy-trained'`/`'mounted-cowboy-trained'` handlers in `setupCowboyVisuals` (now via new `sendUnitToRallyPointIfSet`) and deliberately NOT into `restoreBuildingVisual`'s load-time respawn, so loading a save doesn't re-march every already-garrisoned unit. The rally flag itself is a cheap two-primitive `Graphics` draw (`drawRallyFlag`/`redrawRallyPoints`, new shared `rallyPointGraphics` at depth 6.5) redrawn for every building with a `rallyPoint` set on `'rally-point-changed'`/`'building-removed'`/`'game-loaded'`, following the harvest-ring's minimal-footprint style. `BuildingInfoPanel` gained `formatTrainingQueueText` ("Training: Cowboy (2 ticks left), Cowboy (5 ticks left)" - later jobs show their full seed until they reach the front) and `renderRallyPointControls` (Set/Cancel toggle + a Clear button, shown only once a rally point exists), mirroring its `rallyPointArmedFor` local field off the same `'rally-point-mode-changed'` event MainScene consumes so the button label and the actual armed-click state can't disagree. Persistence (Phase 52): `persistence.ts`'s `cloneBuilding` deep-copies `trainingQueue` (array of job objects) and `rallyPoint` (or `undefined`) alongside its existing `cowboyHp`/`tradeOrders` copies, so a save resumes an in-progress training job's `remainingTicks` exactly where it left off and keeps any rally point - both were meaningful build-progress/config state, not ephemeral. |
| 2026-09-06 | Irrigation & Crop Water Needs | Tier-2 roadmap item. `PotatoField`'s flat `production.outputs` now scales down with water distance, mirroring `wellOutputMultiplier` exactly but as a *soft* falloff rather than Well's hard placement gate: `gameState.getCropWaterDistance(tileX, tileY, type)` calls the existing `distanceToNearestWater` (Phase 50's generalized `distanceToNearestTileType` helper) bounded to a new `WATER_DEPENDENT_CROP_MAX_DISTANCE_TILES` (6), and `getCropOutputMultiplier` indexes a new `CROP_OUTPUT_BY_DISTANCE` table (`[1, 1, 0.85, 0.7, 0.55, 0.4, 0.25]`) off it, returning 0 (not a fallback minimum) once distance is `null` - a bone-dry field yields nothing rather than half rate. Wired into `runProductionTick`'s existing `bonus` multiplier right alongside the Well branch, and into `getPlacementWarning` as a new non-blocking advisory ("Far from water - output reduced to X%" / "...will produce nothing here") - placement itself stays legal per the design brief. New **Water Tower** building (`BuildingType.WaterTower`, 1x1, cost 90, upkeep 0.8, `requiresWorkers: true`, no `production` field, `BuildingCategory.Infrastructure`, `unlockRequirement: { populationAtLeast: 6 }`) is a staffed irrigation relay: its own placement is hard-gated on being within `WELL_MAX_WATER_DISTANCE_TILES` of water by reusing `getWellWaterDistance`/`getPlacementRejection`'s existing Well branch verbatim (same constant, same gate - it relays a real source rather than generating one). Its effect: `getNearestStaffedWaterTowerDistance(tileX, tileY, type)` finds the closest staffed/enabled (hp>0, `!disabled`, `staffed`) Water Tower within a new `WATER_TOWER_IRRIGATION_RADIUS_TILES` (6) of a footprint's center (`getHarvestCenterTile`, Chebyshev distance, same shape as `vegetation.ts`'s radius checks), and `getCropWaterDistance` folds it in as `effectiveDistance = min(actualDistanceToWater, distanceToTower + WATER_TOWER_ASSIST_OFFSET_TILES)` (offset = 1) - i.e. a Water Tower stands in as a virtual water source exactly 1 tile further away than the tower itself, so a PotatoField built beside a distant tower gets near-full output regardless of how far real water actually is, degrading gently the closer to the radius's edge it sits. `BuildingInfoPanel` shows PotatoField's effective water distance, output %, and (when applicable) "irrigated by Water Tower N tiles away", plus Water Tower's own water distance and service radius - both mirroring the existing Well/Cow-Ranch-fence distance-status-line pattern. New `WATER_TOWER_SPRITE` in `BootScene.ts` (cone-roofed tank on a two-post frame with a small blue gauge window) is deliberately not another windmill/crank silhouette, keeping it visually distinct from `WELL_SPRITE`. Persistence (Phase 52): no new `PlacedBuilding` fields were added, so Water Tower round-trips through the existing generic `cloneBuilding`/save-slot machinery unchanged. |
| 2026-09-06 | Random World Events | Tier-2 roadmap item. New standalone `src/state/worldEvents.ts` (matching `market.ts`/`vegetation.ts`'s no-`gameState`-import shape) tracks a single `ActiveWorldEvent` slot (`type`/`startedAtElapsedSeconds`/`expiresAtElapsedSeconds`) over 4 duration-based `WorldEventType`s - `drought`, `goldRush`, `cattleDisease`, `dustStorm` - plus a 5th instant one-shot, `wanderingSettlers`, that never occupies the slot. `pickRandomWorldEventType()` does a weighted pick (`WORLD_EVENT_WEIGHTS`, negative events weighted 3 vs. positive events' 2); `startWorldEvent`/`runWorldEventsTick`/`resetWorldEvents` mirror `market.ts`'s `startMerchantDeal`/`runMarketTick`/`resetMarket` exactly, firing new `'world-event-started'`/`'world-event-ended'` events (`gameEvents.ts`). Four plain 1-or-penalty getters (`getDroughtWellMultiplier` 0.5x, `getCattleDiseaseMultiplier` 0.5x, `getDustStormProductionMultiplier` 0.85x, `getGoldRushMultiplier` 1.5x) are read by `gameState.ts`'s `runProductionTick`/sell passes as an *additional* layer, never a replacement: Drought multiplies straight into the Well branch alongside the existing `wellOutputMultiplier` distance falloff; Cattle Disease multiplies `bonus` whenever `animalConfig` is set (Chicken/Pig/Cattle Farm, Cow Ranch); Dust Storm multiplies `bonus` unconditionally for every producing building (harvesters included, since `harvestOutputs` is also scaled by `bonus`); Gold Rush multiplies the price read in all three sell passes (`runSupermarketSales`/`runSaloonSales`/`runTradingPostSales`, `price = getCurrentMarketPrice(key) * getGoldRushMultiplier()`). New exported `gameState.applyWanderingSettlersReward()` grants a flat `$150-400` (`WANDERING_SETTLERS_MONEY_MIN/MAX`) instead of free population capacity - population is tier-summed from real Houses (Phase 46) and doesn't have a slot for a temporary bonus. Scheduling lives in `MainScene.setupWorldEventSystem`/`scheduleNextWorldEventCheck`/`triggerWorldEvent`, a self-rescheduling timer following `scheduleNextRaidCheck`/`scheduleNextMerchantCheck`'s exact shape (`WORLD_EVENT_MIN/MAX_INTERVAL_MS` 120-200s, wider than the merchant's 90-180s so the two timers don't habitually collide); a roll for a duration-type event is silently skipped if `getActiveWorldEvent()` is already non-null (one active event at a time, like one active raid wave), while `wanderingSettlers` always fires regardless. Every trigger shows a dedicated on-screen banner (`worldEventNoticeText`, a separate `Text` object from `raidNoticeText` so a raid warning and a world-event announcement can coexist, auto-hidden after `WORLD_EVENT_BANNER_DURATION_MS` 6s) and an `addNotification` log entry (`'warning'` for drought/cattleDisease/dustStorm, `'info'` for goldRush/wanderingSettlers). New `src/ui/DustStormOverlay.ts` mirrors `NightOverlay.ts`'s shape exactly (one `setScrollFactor(0)` screen rect, tan tint, depth 899 - just under Night's 900) for Dust Storm's "reduced vision" cosmetic since there's no fog-of-war system to actually dim; it listens to `'world-event-started'`/`'world-event-ended'` directly and snaps (rather than fades) on `'game-reset'`. `resetWorldEventState()`/`resetGame`'s new `resetWorldEvents()` call wire cleanup into the existing reset/game-over paths (`setupGameOverHalt` also cancels `worldEventCheckTimer`) alongside `resetRaidState`/`resetMerchantState`. Persistence (Phase 52): world events are deliberately NOT part of `SaveGameV1` (verified `persistence.ts` has no reference) - `activeEvent` always resets to `null` on a fresh load or reset, exactly like an in-progress raid wave (note: unlike raids, Phase 51's merchant deal *is* persisted via `MarketSaveState.merchantDeal`, so world events follow the raid precedent here, not the merchant one). |
| 2026-09-06 | Objectives / Quest Chain | Tier-2 roadmap item. New standalone `src/config/objectives.ts` (no `gameState` import, matching `resourceGraph.ts`'s pure-derivation shape): `ObjectiveDefinition { id, description, target, unit?, getProgress: (snapshot: ObjectiveSnapshot) => number, reward: { money?, materials? } }` over a fixed, deterministically-ordered `OBJECTIVE_DEFINITIONS` array of 8 - `ship-50-clothes`, `reach-population-10`, `survive-3-nights-clean`, `build-3-watchtowers`, `reach-net-worth-5000`, `ship-20-tools`, `train-5-cowboys`, `sell-30-liquor` - plus `formatObjectiveReward`/`formatObjectiveProgress` display helpers. `gameState.ts` tracks a rolling `ROLLING_OBJECTIVE_SLOT_COUNT` (3, `constants.ts`) active set (`activeObjectiveStates: {id,progress}[]` + an `objectiveQueue: string[]` of the not-yet-active remainder, both seeded by `initObjectiveQueue()` from `resetGame`); `runObjectivesCheck()` (called at the end of `runProductionTick`, right after `checkBuildingUnlocks()`) rebuilds an `ObjectiveSnapshot` every tick, advances each active objective's `progress`, and on reaching `target` grants the reward (money added directly; `materials` added straight to the resource pool bypassing the storage cap - a quest payout, not production output), logs an info-kind `addNotification` ("Objective complete: Ship 50 Clothes -> +$100"), and immediately backfills the freed slot from `objectiveQueue` the same tick via `refillActiveObjectives()`. Three new cumulative counters back objectives that had no existing lifetime-total state (verified none existed - `lastSale`/`saloonSale`/`tradingPostSale` are per-tick only): `cumulativeResourcesSold` (written by a new `addSoldThisRun` call alongside the existing `recordMarketSaleVolume` call in all three of `runSupermarketSales`/`runSaloonSales`/`runTradingPostSales`), `totalUnitsTrained` (incremented in `runTrainingQueues` on job completion, deliberately never decremented like `cowboyCount`/`mountedCowboyCount` are on unit death, so "Train N Cowboys" reflects lifetime investment), and `nightsSurvivedCleanCount` (a `buildingLostThisNight` flag is cleared at the start of every night phase and set by `removeBuilding`'s `reason === 'destroyed'` path when `getDayPhase() === 'night'`; `tickTimer`'s existing phase-transition check increments the count at the following dawn only if the flag never flipped - not required to be consecutive nights). `getActiveObjectives()`/`getCompletedObjectiveCount()` expose a UI-ready view. New always-on (not toggleable, unlike Phase 49's Statistics panel) DOM widget `src/ui/ObjectivesPanel.ts`, docked top-center of `#stage` (`#objectives-panel` in `index.html`, `pointer-events: none` like the Phase 48 resource tooltip) - the one corner left free of `ResourceHudPanel`/minimap (canvas top-left), `BuildingInfoPanel` (bottom-right), `NotificationLogPanel` (bottom-left) and the hidden-by-default `StatisticsPanel` (top-right); re-renders on `'production-tick'`/`'game-reset'`/`'game-loaded'`, showing each active objective as a single "Ship 50 Clothes — 32/50 Clothes" row. Persistence (Phase 52): `SaveGameV1` gained an optional `objectives?: ObjectiveSaveState` field (optional for backward compatibility with pre-Phase-56 saves, which simply keep `resetGame`'s fresh `initObjectiveQueue()` instead) populated by new `gameState.serializeObjectivesState()`/`restoreObjectivesState()`, round-tripping the active/queued objective ids and progress, completed-id set, and all three new cumulative counters. |

| 2026-09-06 | Raider Camps (Offense Phase) | Tier-2 roadmap item - the biggest "AoE feel" item after attack orders, turning the player from pure defender into attacker. New standalone `src/state/raiderCamps.ts` (no gameState import, matching `vegetation.ts`/`market.ts`/`worldEvents.ts`'s shape) holds `RaiderCamp { id, x, y, faction, hp, maxHp }` as plain world-pixel-positioned records (mirrors `Raider.image.x/y`, not tileX/tileY) with `getRaiderCamps`/`getRaiderCampById`/`spawnRaiderCamp`/`damageRaiderCamp`/`removeRaiderCamp`/`resetRaiderCamps` plus a `serializeRaiderCamps`/`restoreRaiderCamps` pair. **Persistence decision**: camps ARE persisted (unlike an in-progress raid wave, which Phase 52 explicitly does not save) - a camp is a standing, slowly-changing map objective rather than active/ephemeral combat state, so `persistence.ts`'s `SaveGameV1` gained an optional `raiderCamps?: RaiderCampSaveState[]` field (same backward-compat pattern as Phase 56's `objectives?`), populated in `serializeGameState`/consumed in `deserializeGameState`; `gameState.resetGame()` calls the new `resetRaiderCamps()` alongside its existing `resetVegetation()` call. **Spawning**: `MainScene.setupRaiderCamps` listens for `'day-phase-changed'` and calls `spawnInitialRaiderCamps()` exactly once per run (guarded by `initialCampsSpawned`, restored correctly from a load via `getDayNumber() >= RAIDER_CAMP_SPAWN_DAY` rather than unconditionally, so a save from before that day still spawns camps naturally later while a later save with 0 camps - all destroyed - doesn't respawn them) the first dawn at/after `RAIDER_CAMP_SPAWN_DAY` (2, `constants.ts`), rolling `RAIDER_CAMP_MIN_COUNT`-`RAIDER_CAMP_MAX_COUNT` (1-3) camps at random `pickRaidSpawnPoint()` edge points with a random `pickRaidFaction()` each, `RAIDER_CAMP_MAX_HP` (140) hp. **Wave sourcing (reuses, doesn't fork, the existing raid system)**: `MainScene.startRaid()` now picks one random live camp (if any exist) and sources that wave's faction/spawn origin from it instead of `pickRaidFaction()`/a random edge point - `spawnRaider` gained an optional `origin` param threaded through for this - so every other piece of wave/combat logic (`runRaidCombatTick`, `resolveRaiderAttacks`, `updateRaiderTargeting`, escalation, the wave timeout/notice) runs completely unmodified; with zero camps on the map, a raid still spawns exactly as before, out of nowhere at a random edge. **Player offense**: `CombatUnit.attackTargetRaiderId: string | null` was generalized to `attackTarget: AttackTargetRef | null` (`{ kind: 'raider' | 'camp'; id: string }`) so an attack order can name either kind; `MainScene.findCampAt` (mirrors `findRaiderAt`) extends the pointerup right-click hit-test (raider checked first, camp second), `issueUnitAttackOrder` takes the generalized ref, `getAttackTargetPosition` resolves either kind's live position each tick, `approachOrEngageRaiderTarget` was generalized to `approachOrEngageTarget(unit, position)`, and `resolveUnitFireTarget` now returns a `{kind:'raider', raider} | {kind:'camp', camp}` union consumed by `resolveCowboyFire`'s damage-application branch (`RaiderCamp.hp` mutated via `damageRaiderCamp`/`damageCamp`, same floored-at-0 convention as `PlacedBuilding.hp`) - a camp is deliberately never an auto-fire/nearest-enemy target (only reachable via an explicit order), keeping Watchtower/default Cowboy defense behavior unchanged. Fixed a latent gap this surfaced: `runRaidCombatTick` used to bail out entirely whenever `raiders.length === 0`, which would have silently no-op'd a player's camp-attack order between raid waves; the guard now also stays open while any unit holds a live camp attack order. Destroying a camp (`MainScene.destroyRaiderCamp`, hp <= 0) removes it via `removeRaiderCamp`, clears any unit's standing order against it, plays the existing dust-burst/collapse-sound pair (`spawnDustBurst` split into a new `spawnDustBurstAt(x,y)` so the burst doesn't require a `PlacedBuilding` to derive a `tileCenter` from), and grants a flat loot reward through new `gameState.grantRaiderCampLoot(money, materials)` (mirrors `runObjectivesCheck`'s reward-granting shape - money direct, materials bypass the storage cap) plus an info-kind `addNotification` ("Outlaws camp destroyed! +$150, +10 Tools"). **Minimap**: camps render as a distinct `MINIMAP_CAMP_COLOR` (purple) marker on the always-on `minimapGraphics` layer (redrawn on `building-placed`/`game-reset`/camp spawn/camp destroy), not the throttled per-frame `minimapCombatGraphics` units/raiders share, so a camp stays visible as a planning target whether or not a wave is active. **Visuals**: new `RAIDER_CAMPS_ATLAS_KEY` atlas in `BootScene.ts` (`generateRaiderCampAtlas`, one frame per faction on a coarser 8x8 grid scaled to `RAIDER_CAMP_SPRITE_SIZE` (24px) - a two-peaked tent silhouette over a small campfire, tent-canvas color varying per faction) plus a per-camp HP bar reusing the Phase 40 unit-HP-bar machinery (`drawUnitHpBar` gained an optional `spriteHalfHeightPx` param so a camp's bigger footprint doesn't collide with its own bar, always shown like a raider's rather than hidden at full health). No camp-side reinforcement/coordination AI was added, matching the phase's explicit scope limit - camps only ever passively source scheduled waves. |
| 2026-09-06 | Unit Roster Expansion with Counters | Tier-2 roadmap item. Two new player units trained at the existing Barracks alongside Cowboy - no new training building, per the phase's scope limit. `UnitKind` (previously redeclared inline three times - `MainScene.ts`'s local type, `gameState.damageUnit`'s param, `TrainingQueueJob.kind`) is now one exported type in `buildingConfig.ts`: `'cowboy' \| 'cowboyOnHorse' \| 'brawler' \| 'dynamiter'`, plus `UNIT_KIND_LABELS`. **Brawler** (melee tank: `BRAWLER_RANGE_TILES` 1/adjacent-only, `BRAWLER_MAX_HP` 60, `BRAWLER_DAMAGE` 14, `BRAWLER_WALK_SPEED_PX_PER_SEC` 35, cost `BRAWLER_TRAIN_COST` $70 + `BRAWLER_TRAIN_MATERIALS` 2 Tools, cap `BRAWLER_MAX_PER_BARRACKS` 3) and **Dynamiter** (fragile lobber: `DYNAMITER_RANGE_TILES` 6, `DYNAMITER_MAX_HP` 18, `DYNAMITER_DAMAGE` 10, `DYNAMITER_WALK_SPEED_PX_PER_SEC` 40, cost $100 + 3 Tools, cap `DYNAMITER_MAX_PER_BARRACKS` 2, fire rate deliberately left uniform with every other unit - a documented simplicity choice) join Cowboy/Cowboy-on-Horse's `COWBOY_*`/`MOUNTED_COWBOY_*` constant families in `constants.ts`. **PlacedBuilding** gained `brawlerCount`/`brawlerHp`/`dynamiterCount`/`dynamiterHp`, a third and fourth parallel count+HP-array pair extending the existing `cowboyCount`/`cowboyHp` convention rather than a `Record<UnitKind, {count,hp[]}>` refactor of the ~37 existing call sites across `gameState.ts`/`MainScene.ts`/`BuildingInfoPanel.ts`/`persistence.ts` (documented tradeoff in the field's own doc comment) - three new `buildingConfig.ts` helpers, `getUnitHpArray`/`getUnitCount`/`setUnitCount`, are the single place that switches on `UnitKind` to pick the right field pair, used by `gameState.damageUnit`/`trainBrawler`/`trainDynamiter`/`runTrainingQueues` and `MainScene.isCowboyUnitAlive`/`getUnitHp`. **`UNIT_KIND_CONFIG`** (`MainScene.ts`) is now genuinely per-kind rather than speed/HP-only: `rangeTiles`/`damage`/optional `splashRadiusTiles`+`splashDamage` too (Cowboy-on-Horse deliberately keeps Cowboy's exact range/damage - only speed/HP still differ there, unchanged from Phase 28). `resolveCowboyFire`'s per-unit range read plus a new `applyUnitDamage`/`applyDynamiterSplash` pair replace the old bare `-= COWBOY_DAMAGE`: every hit resolves `getFactionUnitDamageMultiplier(faction, kind)` first, and a Dynamiter's hit additionally splashes `DYNAMITER_SPLASH_DAMAGE` (each independently multiplier-scaled by ITS OWN target's faction) to every other live raider/camp within `DYNAMITER_SPLASH_RADIUS_TILES`. **Counter table** `FACTION_UNIT_DAMAGE_MULTIPLIER: Record<RaiderFaction, Partial<Record<UnitKind, number>>>` (`buildingConfig.ts`, default 1.0 when absent, via `getFactionUnitDamageMultiplier`): Outlaws 0.85x vs. Cowboy / 1.4x vs. Brawler (tanky generalist resists gunfire, weak to melee); Rustlers 1.35x vs. Dynamiter splash; Coyotes 1.3x vs. Dynamiter splash (their real Brawler counter is the mobility already implicit in `RAIDER_DEFINITIONS.speedPxPerSec`, deliberately not duplicated as a damage multiplier). Spawning/slots: new `spawnUnitOfKind`/`getSquareUnitSlotPosition`/`UNIT_VISUAL_CONFIG`/`UNIT_KIND_SLOT_OFFSET` in `MainScene.ts` replace the old `spawnCowboyUnit`/`spawnMountedCowboyUnit`/`getCowboySlotPosition` pair so all three square kinds (Cowboy/Brawler/Dynamiter, all `COWBOY_SPRITE_SIZE`) share one spawn/slot-layout function without colliding on the same Barracks' slot grid (each kind reserves a fixed offset block sized to its own per-Barracks cap); `restoreBuildingVisual` respawns all three garrisoned kinds on load. Training: `gameState.trainBrawler`/`trainDynamiter` mirror `trainCowboy` but also gate on and deduct `BRAWLER_TRAIN_MATERIALS`/`DYNAMITER_TRAIN_MATERIALS` (new `hasEnoughResourcesFor`/`deductResources` helpers) alongside money; `runTrainingQueues` generalized from a two-way if/else to a `switch` over all four kinds, firing new `'brawler-trained'`/`'dynamiter-trained'` events (`gameEvents.ts`) MainScene listens for exactly like the existing two. `BuildingInfoPanel` gained `renderTrainBrawlerButton`/`renderTrainDynamiterButton` (Barracks-only, alongside the existing Train Cowboy button) and `formatTrainingQueueText` now labels queued jobs via `UNIT_KIND_LABELS` instead of a two-way ternary. Persistence: `persistence.ts`'s `cloneBuilding` deep-copies `brawlerHp`/`dynamiterHp` alongside the existing `cowboyHp`/`mountedCowboyHp` clones - no `SaveGameV1` shape change needed since these are just two more `PlacedBuilding` fields on the existing per-building save path. New `BRAWLER_SPRITE` (buzz-cut + bright knuckle pixels punched out to both sides of the torso - a fists motif) and `DYNAMITER_SPRITE` (satchel patch across the torso + a lit-fuse spark pixel above the hat) in `BootScene.ts`, each its own single-frame square atlas (`generateBrawlerAtlas`/`generateDynamiterAtlas`) at the same small-unit size class as Cowboy. |
| 2026-09-06 | Ambient Soundtrack & Soundscape | Tier-2 roadmap item. `audio/sound.ts` gains a second always-on bus alongside the existing SFX `masterGain`: a `musicGain` GainNode feeding `ctx.destination` in parallel, built once in `ensureMusicGraph` (called from `getAudioContext`) with a lower `MUSIC_VOLUME_DEFAULT` (0.32 vs. SFX's 0.6) and two `CrossfadeBus` pairs underneath it - `musicMelodyBus`/`musicAmbientBus`, each a `{day, night}` GainNode pair. Both variants of both loops schedule continuously via their own `setTimeout` chains (`scheduleMelodyLoop`/`scheduleAmbientLoop`, deliberately plain JS timers rather than Phaser's `this.time`, keeping the module scene-independent per its existing `listenerRect`-is-pushed-in convention) and are always "running", just silent at gain 0 - a day/night boundary is a `crossfadeBusToPhase` linear ramp on already-live nodes over the existing `DAY_NIGHT_TRANSITION_MS`, mirroring `NightOverlay.ts`'s alpha-tween crossfade but for gain, triggered by the same `'day-phase-changed'`/`'game-reset'` events (subscribed at sound.ts module scope, snapping to day immediately on reset like `NightOverlay` does). The melody (`MELODY_VARIANTS`, `renderPluckedNote` - short-attack/exponential-decay lowpass-swept oscillator over a shared `PENTATONIC_SCALE_HZ`) is brighter/faster/upper-register by day, slower/sparser/lower-register by night (each beat only ~30-55% likely to actually play a note, per `noteProbability`, keeping it ambient rather than a tune); the soundscape (`AMBIENT_VARIANTS`, `renderWindGust` a slow quiet bandpass noise swell for day, `renderCricketChirp` a burst of short high-pitched pulses for night) is a distinct, lower-volume (`AMBIENT_NOTE_GAIN` 0.05 vs. melody's 0.09), always-on background layer separate from Phase 36's per-animal ambience timer. Neither loop's notes touch `activeVoices`/`MAX_CONCURRENT_VOICES` - that SFX budget exists to stop a raid volley from clipping into mush, not to throttle a background bus, a distinction called out in the file's own doc comment. Music only starts (`startMusicIfNeeded`) the first time `installAudioUnlock`'s resume actually reaches `ctx.state === 'running'`, so there is no attempted autoplay ahead of the existing pointerdown/keydown gesture gate; `effectiveMusicGain()` mutes at the same `muted`/`gameSpeed === 0` conditions as SFX, sharing the single mute toggle. New `raidStinger` `SoundName` (`renderRaidStingerChord`, two sawtooth voices a minor second apart sliding upward) fires once per wave from `MainScene.startRaid` via new export `playRaidStinger()`, throttled 3s apart via the ordinary `minGapMs` mechanism; its `render` additionally calls `duckMusicForStinger`, linear-ramping `musicGain` down to `MUSIC_DUCK_LEVEL_FACTOR` (0.25x) and back up over the stinger's `RAID_STINGER_DURATION_S` (1.1s) plus a `MUSIC_DUCK_RELEASE_S` tail, independent of the day/night crossfade weights. New exports `setMusicVolume`/`getMusicVolume`; `'audio-settings-changed'` (`gameEvents.ts`) widened to carry `musicVolume` alongside the existing `muted`/`volume`. `BuildingBar.createAudioControls` gained a second range input (`musicVolumeSlider`, class `volume music-volume`, distinct accent color in `index.html`) next to the existing SFX slider, sharing the one mute button rather than getting its own. |
| 2026-09-06 | Goods Carts on Roads | Tier-2 roadmap item, purely cosmetic - no change to the existing +10% connected-production bonus or Roads & Logistics' BFS connectivity rules themselves. `MainScene.setupGoodsCarts` listens to the existing `'production-tick'` event (fired at the end of `gameState.runProductionTick`, after every `PlacedBuilding.active`/`connected` flag for the tick is already fresh): for every building that just produced (`active === true`, set at the exact `recordProductivityTick(id, true, null)` call site added back in Phase 49) and is road-connected (`connected`, Roads & Logistics' existing `isBuildingConnected` BFS), it spawns a one-shot cart sprite via `spawnGoodsCart(from, to)` traveling to the nearest connected, undamaged depot (`CART_DEPOT_BUILDING_TYPES` = Warehouse/Supermarket/Saloon/TradingPost, picked by straight-line distance in `findNearestCartDepot` - none of the four have a `production` field, so a depot can never itself be a cart's source). Movement reuses villagers' exact point-to-point tween technique (`startVillagerWander`/`sendRaiderToTarget`'s distance/speed -> duration, `setFlipX` for facing) but one-shot: no `onComplete` chain into another leg, since a cart represents a single delivery - it fades out (`CART_FADE_OUT_DURATION_MS` 300ms tween to alpha 0) and destroys itself on arrival, following Phase 38's "simple robust behavior over full pathfinding" precedent (a direct tween, not real road-tile pathfinding). Concurrency capped at `MAX_VISIBLE_CARTS` (8, tracked via `MainScene.activeCarts`) - a spawn attempt past the cap is silently skipped rather than queued, matching Phase 20 villagers' "capped rendered sprites" precedent; create-then-destroy-on-arrival is used instead of pooling since carts are already short-lived one-shots. Cleanup follows the exact `killTweensOf`-then-`destroy()` discipline `setupGameReset` already applies to villagers/accents/animals. New single-frame, non-square `CARTS_ATLAS_KEY` atlas (`BootScene.generateCartAtlas`, same technique as `generateMountedCowboyAtlas`) holds a `CART_SPRITE` wagon silhouette (tan cargo crates over a dark wood bed on two black wheel hubs) at `CART_SPRITE_WIDTH`x`CART_SPRITE_HEIGHT` (14x10, wider-than-tall like Cowboy-on-Horse) - a distinct asset class from animals/villagers/units, all defined in `buildingConfig.ts`. |

## License & Repo Info

- **License**: MIT (recommended for open-source projects)
- **Language**: English (README, comments), Code in English
- **Repo Name**: `western-village-phaser`